# 오늘 할일 - 2026년 4월 26일

## M100 — v1.0.0 조판 엔진

| Issue | 타스크 | 상태 | 비고 |
|------|--------|------|------|
| #338 | Firefox AMO 워닝 해결: strict_min_version 상향 + viewer 번들 보안 워닝 검토 | 완료 | 완료: 03:42, AMO reviewer note 작성 |
| #347 | 표/그림 절대 좌표 계산 오류 수정 (HorzRelTo::Page, HorzAlign::Right, 글뒤로 그림 y 진행, TAC 그림 렌더 누락, InFrontOfText 표 push-down, TAC 그림/표 z-order) | 완료 | exam_eng.hwp p2 우측 박스, p4 우측 Q27/Q28 박스 정상화 |
| #354 | rhwp-firefox/build.mjs CodeQL 경고 (execSync → execFileSync) | 완료 | Code Scanning Alert #17 해소 |
| #359 | 페이지네이션 fit 판정 vs Layout y 진행 정합 (k-water-rfp 311px drift) | 완료 | typeset.rs fit 누적을 total_height 분리 + 단독 항목 페이지 차단 가드. k-water-rfp 73→0, kps-ai 60→4 LAYOUT_OVERFLOW. 1008 lib + 6 svg_snapshot 통과 |
| #377 | API Export 기능 제안 (서버 업로드 가이드 요청) | 완료 (close) | bytes 반환 API (`exportHwp() / exportHwpx()`) 이미 제공. 위키에 [Export API 사용 가이드](https://github.com/edwardkim/rhwp/wiki/Export-API-%EC%82%AC%EC%9A%A9-%EA%B0%80%EC%9D%B4%EB%93%9C) 추가 (서버 업로드 / 로컬 / 진행률 / 인증 / HWPX / Rust native 시나리오 6건). 댓글 안내 + close |
| #375 | 원클릭 실행 스크립트 추가 제안 (@NoteBlockMR) | 완료 (close) | 위키에 [Cloudflared 로 rhwp-studio 외부 HTTPS 접근](https://github.com/edwardkim/rhwp/wiki/Cloudflared-%EB%A1%9C-rhwp-studio-%EC%99%B8%EB%B6%80-HTTPS-%EC%A0%91%EA%B7%BC) 가이드 흡수 (작성자 attribution 보존). zip 직접 흡수는 vite.config 분기 / 포트 변경 / dev server expose 보안 우려로 거절. PR 형태 재제출 시 검토 가능 안내 + close |
| #383 | rhwp-studio PWA 설치 지원 제안 (@dyjung150605) | PR 대기 (OPEN) | vite-plugin-pwa@1.2.0 (peer ≤vite7, `legacy-peer-deps` 우회). 작성자가 4 환경 사전 검증 + CONTRIBUTING.md 의향 확인 절차 준수. 수락 + PR 권유 (manifest scope `/rhwp/` / icon 192·512·maskable / registerType / WASM precache / E2E 회귀 등 7개 점검 항목 안내). enhancement 라벨 추가 |
| #364 | PageLayerTree 생성 API 추가 제안 (@postmelee, #363 후속) | PR 대기 (OPEN) | #363 → PR #385 (v0.7.7) 흡수 흐름의 후속. 수락 + PR 권유. 6개 점검 / 합의 항목 안내 (schema 정리 / schemaVersion 정책 / 변환 정확성 / PR 범위 좁히기 (변환 API 만, renderer 제외) / surface 우선순위 (Rust + WASM 먼저, C ABI 후속) / 회귀 테스트). enhancement 라벨 추가 |
| #394 | rhwp-studio 셀 진입 시 투명선 자동 ON 로직 비활성화 (한컴 출력 정합) | 완료 | input-handler.ts 5 영역 (호출 지점 2 / 메서드 본체 / 상태 변수 3 / 이벤트 핸들러) 주석 처리 — `[Task #394]` 마커로 향후 되돌리기 가능. 1016 lib + 6 svg_snapshot + clippy + Vite 빌드 통과. 작업지시자 시각 판정 통과 |
| #416 | find_bin_data 가드 결함 (Task #195 회귀, 페이지 배경 이미지 잘못 표시) | 완료 | `c.id == bin_data_id` 가드 제거 — `c.id` 는 storage_id, bin_data_id 는 인덱스라 정상 케이스 거짓 실패 (트러블슈팅 #2026-02-17 결함 패턴 재발). 가드 제거 + sparse id 범위 분기 (HWPX 차트 60000+N 보존). 단위 테스트 7개 추가 (1016→1023). hwpspec.hwp 1페이지 페이지 표지 정상 (16×13 → 793×1121). WASM 빌드 통과. 트러블슈팅 문서 회귀 이력 + 추가 교훈 보강. 작업지시자 시각 판정 통과 |
| #418 | hwpspec.hwp 20페이지 빈 문단 + TAC Picture 이중 emit 회귀 (Task #376 머지 누락) | 완료 | Task #376 (commit 45419a2) 정정이 devel 미머지 (이슈 close 됐지만 임시 브랜치에만 존재) → 동일 결함 재발. paragraph_layout 의 Picture push 후 set_inline_shape_position 호출 + layout.rs::layout_shape_item 분기에 already_registered 가드 추가. tests/issue_418.rs 회귀 테스트 신설. lib 1023 + svg_snapshot 6/6 + clippy + WASM 빌드 통과. p20 image 6→3. 신규 메모리 (close 시 commit devel 머지 검증) + 신규 트러블슈팅 (TAC Picture 이중 emit). 작업지시자 시각 판정 통과 |
| PR #395 | 그림 밝기/대비 효과 SVG 반영 (#150, @oksure) | 완료 (cherry-pick + 후속 정정) | PR #387 → #395 devel 기반 재제출. cherry-pick (cb248cd, e637687) + 메인테이너 후속 정정 (c9fe462): 누락 들여쓰기 / i8 clamp / 수치 테스트 3개. lib 1023→1029, svg_snapshot 6/6, Task #418 회귀 테스트 보존, clippy 0, WASM 빌드 통과. HashSet dedup 별도 PR 후보 안내 |
| PR #396 | 수식 렌더링 개선 — TAC 높이 + 한글 이탤릭 (#174, #175, @oksure) | 완료 (cherry-pick + 후속 정정) | PR #388 → #396 devel 기반 재제출. cherry-pick (5cf2b79, dbaa74b) + 메인테이너 후속 정정 3건 (Canvas 분수선 y / web_canvas Equation 스케일 / Canvas Limit fi=fs). web 에디터 vs SVG 비교에서 Canvas 결함 식별 후 정정. lib 1029→1031, svg_snapshot 6/6, issue_418 보존, clippy 0, WASM 빌드 통과. 작업지시자 시각 판정 통과 |
| PR #397 | 수식 ATOP 파싱 및 렌더링 보정 (@cskwork) | 작성자 재제출 대기 (OPEN) | 신규 컨트리뷰터 첫 PR. 코드 자체는 합리적 (ATOP / Fraction 분리, 분수선 없이 위/아래 배치). dry-run merge + 1033 passed 검증 통과. 옵션 B (작성자 절차 안내): devel 기반 rebase + 시각 검증 자료 (한컴 출력 vs rhwp 출력) + CI 통과 요청. 향후 지속 기여를 위한 절차 학습 기회 |
| PR #400 | HWPX 수식 직렬화 보존 (#286, @cskwork) | 작성자 재제출 대기 (OPEN) | 이슈 #286 milestone v1.0.0 으로 갱신. 코드 자체는 합리적 — render_paragraph_parts 가 controls 무시하던 정황 정정 + parser 측 XML entity 복원. dry-run merge + 1034 passed 검증 통과. 옵션 A (작성자 보강 요청): 자기검증 테스트 한계 (메모리 feedback_self_verification_not_hancom 위반) — 한컴 hwpx 샘플 활용 회귀 테스트 + 한컴 편집기 열람 증빙 + devel rebase + CI 통과 요청 |
| PR #401 | 표 페이지 분할 rowspan>1 셀 분할 단위 산정 누락 정정 (#398, @planet6897) | 작성자 재정정 대기 (OPEN) | 신뢰 컨트리뷰터, CLAUDE.md 절차 준수, CI 통과, cherry-pick 후 1038 passed. 본 샘플 시각 통과. 그러나 메인테이너 회귀 샘플 samples/synam-001.hwp 5페이지에서 회귀 발견 — pi=69 의 셀[4] (rs=5) 큰 rowspan 셀이 PR 의 rowspan 블록 단위 분할로 페이지 5 잔여 공간에 안 들어감 → 35→37 페이지 증가. 옵션 A: synam-001.hwp 를 devel 에 commit (b1c97fe) + 작성자 재정정 요청 (큰 rowspan 셀 가드 보강) |
| PR #405 | Paragraph::control_text_positions 추가 (옵션 A, #390, @DanMeon) | 완료 (cherry-pick 머지) | 작업지시자 사전 PR 권유 사안. 이슈 작성자 본인 정정. 알고리즘 위치 이동 리팩토링 (변경 없음, 외부 binding 노출 위함). cherry-pick 3 commit + 1031→1037 passed + clippy 0 + WASM 빌드 통과. API 노출이라 시각 판정 불필요 — 단위 테스트 + cargo test 로 검증 게이트 통과 |
| PR #411 | editor.exportHwp() API 추가 (@ggoban) | 완료 (cherry-pick 머지) | 신규 컨트리뷰터 첫 PR. iframe wrapper @rhwp/editor 에 exportHwp() 노출 — WASM core 는 이미 있고 message channel 경로만 추가. 4 files / +30 / -0, Rust core 영향 없음. cherry-pick 단일 commit. lib 1037 passed (동일) + tsc + clippy 0. 이슈 #377 위키 가이드의 후속. API 노출이라 시각 판정 불필요 |
| PR #413 | rhwp-studio PWA support (#383, @dyjung150605) | 완료 (cherry-pick 머지) | 신규 컨트리뷰터 첫 PR. 이슈 #383 사전 의향 확인 + 메인테이너 안내 7항목 100% 대응 (start_url/scope, icon 192+maskable, registerType=autoUpdate 사유, legacy-peer-deps 임시성, WASM runtimeCaching 분리, E2E devOptions=false, deploy-pages.yml 무변경). 4 환경 + fork 배포 사전 검증. cherry-pick 2 commit + 1037 passed + tsc + clippy 0. PWA 설치 검증은 deploy-pages.yml 자동 트리거 |
| PR #415 | Task #352 dash 시퀀스 Justify 폭 부풀림 정정 (#352, @planet6897) | 완료 (cherry-pick 머지) | 신뢰 컨트리뷰터, 5 단계 진단 + PDF 정량 비교. 본 PR 의 40 commits 가 다른 OPEN PR (#401 등) 변경 누적 — 옵션 A 로 Task #352 핵심 7 commits 만 분리 cherry-pick 하여 PR #401 의 synam-001 회귀 회피. cargo test --lib 1031→1037 passed + svg_snapshot 6/6 + clippy 0 + WASM 빌드. exam_eng Q32 dash advance 12.11 → 7.06 px (PDF ~7.4 px). 작업지시자 시각 판정 통과 (dash 시퀀스 처리 정상) |
| PR #401 v2 | Task #398 표 페이지 분할 rowspan>1 v2 정정 (#398, @planet6897) | 완료 (cherry-pick 머지) | 1차 검토에서 synam-001.hwp 5페이지 회귀 발견 (35→37 페이지) → 작성자 재정정 요청. v2: BLOCK_UNIT_MAX_ROWS=3 임계, 작은 블록 (≤3 행) 만 보호 / 큰 rowspan (≥4 행) 행 단위 분할 허용 (HanCom 호환). cherry-pick 5 commits + 1037→1044 passed + svg_snapshot 6/6 + clippy 0 + WASM 4,127,757 bytes. synam-001 35페이지 정상 + 본 샘플 페이지 분할 정상. 작업지시자 시각 판정 통과 |
| PR #397 | 수식 ATOP 파싱 및 렌더링 보정 (@cskwork) | 완료 (cherry-pick 머지) | **본 저장소 첫 외부 컨트리뷰터**. 1차 검토 옵션 B 절차 안내 → 작성자가 rebase + 시각 자료 + CI 정황 모두 정확히 대응. 메인테이너 GitHub Actions API 로 첫 외부 PR CI 승인 → CI Build & Test SUCCESS. cherry-pick 단일 commit + 1044→1046 passed + svg_snapshot 6/6 + clippy 0 + WASM 4,134,499 bytes. samples/atop-equation-01.hwp (작업지시자 추가 전용 샘플) 시각 판정 통과 — ATOP 분수선 0개 / OVER 분수선 1개 (회귀 없음) / ATOP 그룹 0개 모두 정상 |
| PR #400 | HWPX 수식 직렬화 보존 (#286, @cskwork) | 완료 (cherry-pick 머지) | @cskwork 의 두 번째 PR. 1차 검토 옵션 A 보강 요청 → 작성자가 5 항목 모두 정확 대응 (한컴 origin 회귀 commit `ecd7d9a` 추가 + 한컴 한글 2020 PDF 검증 자료 + linear rebase). 메모리 feedback_self_verification_not_hancom 원칙 정확히 회피. cherry-pick 2 commits + 1046→1050 passed + svg_snapshot 6/6 + clippy 0 + WASM 4,114,661 bytes. 한컴 한글 2020 정상 열람 + PDF 일치. HWPX 직렬화 (저장 경로) 정정이라 시각 판정 불필요 |
| #174, #175 | 수식 TAC 높이 + CASES 한글 겹침 (PR #396 흡수, devel `0d51736`) | 완료 (close) | PR #396 cherry-pick 머지 시 GitHub 자동 close 미트리거 (cherry-pick 정황) → 메인테이너 수동 close. devel 머지 commit 검증 후 처리 |
| PR #406 | 동일 문단 inline TAC 그림 페이지네이션 정정 (#402, @planet6897) | 완료 (cherry-pick 머지) | 신뢰 컨트리뷰터. PR 의 8 commits 중 Task #398 (3) 은 PR #401 v2 로 흡수 완료 → Task #402 핵심 3 commits (`0054a27`, `38bea10`, `f710732`) 분리 cherry-pick. 1050 passed (회귀 없음) + svg_snapshot 6/6 + issue_418 1/1 + clippy 0 + WASM 4,115,251 bytes. 한컴 정답지 vs rhwp 비교: 27→30 페이지 (분할 정상화), 7쪽 표+파이 차트 겹침 해소, 8쪽 파이 차트 정상 배치. SVG + Canvas (rhwp-studio) 양 경로 작업지시자 시각 판정 통과 |
| PR #408 | heading-orphan vpos 기반 보정 (#404, @planet6897) | 완료 (cherry-pick 머지) | 신뢰 컨트리뷰터. PR 의 14 commits 중 Task #398 (3) PR #401 v2 흡수 + Task #402 (3) PR #406 흡수 + 샘플 PDF (1) no-op → **Task #404 핵심 4 commits** (`36a457c`, `25b27b2`, `42b5136`, `3a1c9d6`) 분리 cherry-pick. 본질: vpos 기반 5 조건 AND trigger (current fit + vpos overflow + next substantial + next 못 fit + single column non-wrap) — vpos overflow 41건 중 1건만 진짜 orphan (조건 E 가 핵심 필터). 1050 passed + svg_snapshot 6/6 + issue_418 1/1 + clippy 0 + WASM 4,124,155 bytes (+8,904). 9쪽 pi=83 헤딩 → 10쪽으로 push, 후속 표 pi=84/85 와 함께 배치 (한컴 PDF 일치). SVG + Canvas 양 경로 시각 판정 통과 |
| #426 | 이미지 + 캡션 후 다음 문단이 캡션과 오버래핑 (aift.hwp 19페이지) | 신규 등록 | 작업지시자 발견 (PR #408 후속). v1.0.0 마일스톤. 재현: `rhwp export-svg samples/aift.hwp -p 18`. 캡션 높이가 후속 문단 누적에 미반영 가능성. PR #410 회귀 영향 없음 검증 (byte 단위 동일) |
| PR #410 | TopAndBottom Picture vert=Para chart 정정 + atomic TAC top-fit (#409, @planet6897) | 완료 (cherry-pick 머지) | 신뢰 컨트리뷰터. PR 의 27 commits 중 Task #398/#402/#404 commits 는 이미 흡수 + 샘플/계획서/Stage/merge → **Task #409 본질 3 commits** (v1 `368a869` layout 가드 확장 / v2 `d1f19e9` typeset_section chart 누적 / v3 `fa8f923` typeset_paragraph atomic TAC top-fit 60px tolerance) 분리 cherry-pick. 1050 passed + svg_snapshot 6/6 + issue_418 1/1 + clippy 0 + WASM 4,116,929 bytes. 21~24쪽 PDF 정합 (21쪽 2x1 표 차트 직하 / 22쪽 (4) 헤딩+10x5 표 / 23쪽 막대 차트 하단 / 24쪽 2x1 표→(6) 헤딩→파이차트). SVG + Canvas 양 경로 시각 판정 통과. #426 회귀 영향 없음 (byte 동일). 회귀 검증 중 #429 발견 (별도 등록) |
| #429 | 표 안 이미지 미조판 (aift.hwp 41페이지) | 신규 등록 | 작업지시자 발견 (PR #410 회귀 검증 중). 사전 결함, 본 PR 변경 영역 외. v1.0.0 마일스톤. 재현: `rhwp export-svg samples/aift.hwp -p 40`. 표 셀 내 Picture 컨트롤 SVG 출력 누락 |
| PR #419 | PageLayerTree generation API 도입 (#364, @seo-rii) | 완료 (cherry-pick 머지) | 신규 외부 컨트리뷰터 @seo-rii (PR #165 분할 재제출의 1단계). 이슈 #364 작성자 @postmelee 와 두 컨트리뷰터 합의 기록 메인테이너 직접 확인. 메인테이너 안내 7항목 정확 대응. PR 의 9 commits 중 merge commit 제외 본질 8 commits 분리 cherry-pick. 본질: paint 모듈 신규 (2,376 lines, builder/json/layer_tree/paint_op) + opt-in transition adapter (svg_layer.rs, RHWP_RENDER_PATH=layer-svg). 1050→1062 passed (+12 paint 단위 테스트) + svg_snapshot 6/6 + issue_418 1/1 + clippy 0 + WASM 4,184,496 bytes (+67,567). **피델리티 분석** (작업지시자 요청): 기존 5 렌더러 파일 (svg.rs, canvas.rs, web_canvas.rs, render_tree.rs, layout.rs) 변경 0 라인, paint 미의존, wasm_api.rs 메서드 251→252 (1개만 신규), **10 샘플 309 페이지 SVG byte 단위 100% 동일**. layer-svg opt-in 경로도 legacy 와 byte 동일. API 추가 패턴이라 시각 판정 불필요 |
| PR #424 | 다단 우측 단 단행 문단 줄간격 누락 정정 (vpos 보정 anchor, #412, @planet6897) | 완료 (cherry-pick 머지) | 신뢰 컨트리뷰터. PR 의 36 commits 중 Task #398/#402/#404/#409 commits 다른 PR 흡수 완료 → **Task #412 본질 4 commits** (`144ff53`, `cdeea6b`, `3d395e2`, `a6c2457`) 분리 cherry-pick. 본질: layout.rs vpos 보정 공식 정정 (col_anchor_y 도입 — body_wide_reserved 푸시 직후 anchor 보존, curr_first_vpos 우선 사용, page_path/lazy_path 분리). 1062 passed + svg_snapshot 6/6 + issue_418 1/1 + clippy 0 + WASM 4,184,640 bytes (+144). 광범위 byte 비교 309 페이지 중 71 페이지 변화 (의도된 다단 정정). exam_eng p1 우측 단 item 7 ①~⑤ 15.33→22.55px 균일, 좌측 단 item 1 catch-up 28.56→21.89, p2 item 20 정상화. SVG + Canvas 양 경로 작업지시자 시각 판정 통과 (메모리 v076_regression_origin 원칙 부합 — 광범위 변화에도 직접 시각 검증 게이트) |
| #431 | synam-001.hwp 15페이지가 빈 채로 16페이지로 밀림 (PR #424 후 회귀) | 신규 등록 | 작업지시자 발견 (PR #424 머지 후 회귀). v1.0.0 마일스톤. 재현: `rhwp export-svg samples/synam-001.hwp -p 14`. 15페이지 SVG 3.3 KB (빈 페이지), 16페이지 337 KB (밀려온 표). PR #424 의 vpos 보정 정정 (col_anchor_y / page_path / lazy_path) 이 synam-001 표 fit 결정에 영향 가능성. 정정 시 PR #424 의 다른 정정 (exam_eng p1 22.55px 균일 등) 보존 필요 |
| PR #427 | SvgRenderer defs 중복 방지 HashSet 통합 (#423, @oksure) | 완료 (cherry-pick 머지) | 신뢰 컨트리뷰터, PR #395/#396 머지 통과. PR #395 리뷰 시 메인테이너 안내 사안 → 작성자가 즉시 PR 화. 단일 파일 단순 리팩토링 (svg.rs +8/-10). `arrow_marker_ids: HashSet<String>` → 범용 `defs_ids: HashSet<String>` 통합 + ensure_image_effect_filter / ensure_brightness_contrast_filter O(n)→O(1). 단일 commit cherry-pick. 1062 passed + svg_snapshot 6/6 + issue_418 1/1 + clippy 0 + WASM 4,178,840 bytes (-5,800, 코드 정리 효과). 광범위 byte 비교 309/309 동일 — 자료구조 변경만, 출력 영향 없음. 리팩토링 패턴이라 시각 판정 불필요 |
| PR #428 | 그룹 내 그림(Picture) 직렬화 구현 (@oksure) | 작성자 보강 대기 (OPEN) | 신뢰 컨트리뷰터. `serialize_group_child::Picture` 빈 TODO → Chart/OLE 패턴 동일하게 채움 + `0x24706963` 매직넘버 → `tags::SHAPE_PICTURE_ID` (control.rs +14/-3 단일 파일). 호출 함수들 (serialize_picture_data / serialize_shape_component / serialize_common_obj_attr) 은 트러블슈팅 picture_save_hancom_compatibility.md 정정 적용 상태. dry-run cherry-pick + 1062 passed + clippy 0 통과. 그러나 **저장 (직렬화) 경로** 라 메모리 feedback_self_verification_not_hancom 원칙 (자기 라운드트립 ≠ 한컴 호환) 에 따라 한컴 호환 검증 자료 필요. 옵션 A (작성자 보강 요청): 한컴 origin 기반 회귀 테스트 추가 (PR #400 의 equation_roundtrip_from_hancom_origin_hwp_sample 패턴) + 한컴 한글 2010/2022 정상 열람 + PDF 비교 자료. 작업지시자 코멘트: "자기 라운드 트립 코드 즉, 무조건 참인 케이스가 아닌지 리뷰해야 한다는 의미" |
