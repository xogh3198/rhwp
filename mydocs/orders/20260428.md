# 오늘 할일 - 2026년 4월 28일

## M100 — v1.0.0 조판 엔진

| Issue | 타스크 | 상태 | 비고 |
|------|--------|------|------|
| #398 | 표 페이지 분할 시 rowspan>1 셀이 분할 단위 산정에서 누락 (2025년 기부·답례품 보고서 양식 1쪽 하단) | 완료 | `MeasuredTable.row_block_start/end` + `compute_row_blocks` + `row_block_for`/`snap_to_block_boundary` 추가. `pagination/engine.rs::split_table_rows` + `typeset.rs::paginate_table` (실제 SVG 경로) 양쪽에 블록 정책 적용. 본 샘플 1쪽 표 분할 사라지고 2쪽 시작 (PDF/한글과 동일). 1023 lib + 6 svg_snapshot + 50 통합 = 1073 통과, release 빌드 성공 |
| #417 | HWP 3.0 정식 파서 구현 — parse_hwp3() → Document IR | 진행 중 | `src/parser/hwp3/` untracked → tracked 전환, debug println! 제거, 하이퍼링크 URL 추출 구현 |

## 버그

| Issue | 타스크 | 상태 | 비고 |
|------|--------|------|------|
| #402 | 동일 문단 내 두 번째 line의 inline 그림이 첫 line과 겹침 (2025년 기부·답례품 보고서 양식 7쪽 파이차트) | 완료 | `layout.rs::layout_shape_item`: 선행 TAC 컨트롤이 있으면 `para_start_y`를 진행된 `y_offset`으로 갱신 → 그림 y가 표 아래로 이동. `typeset.rs::typeset_table_paragraph` (실제 SVG 경로): 선행 TAC 그림의 line_seg lh를 `current_height`에 누적 + 페이지 초과 시 분할 → 7쪽 표만, 8쪽에 파이차트. 1023 테스트 통과, 10개 샘플 LAYOUT_OVERFLOW 카운트 회귀 없음 |
| #404 | 헤딩 문단이 후속 콘텐츠와 다른 페이지로 분리됨 (2025년 기부·답례품 보고서 양식 9쪽 pi=83 "(7) 다수 기부자 현황") | 완료 | `typeset.rs::typeset_section` 메인 루프에 vpos 기반 heading-orphan trigger 추가. 5개 조건 AND (current fits + vpos overflow > 1mm + next substantial > 30px + next doesn't fit + single column non-wrap) 로 false positive 차단. page_top_vpos 는 current_items 첫 item para_index 로 즉시 계산. 1073 테스트 통과, 10개 샘플 LAYOUT_OVERFLOW 회귀 없음 + 타겟 -15 / kps-ai -1 개선 |
| #409 | 21페이지 2x1 표 위치 오류 (TopAndBottom Picture 다음 문단 vpos 보정 차트 높이 이중 반영) | 완료 | `prev_has_overlay_shape` 가드를 Control::Picture (non-TAC) + TopAndBottom/vert=Para 케이스로 확장. 21페이지 2x1 표가 차트 바로 아래 정상 위치 복원 (PDF 일치). LAYOUT_OVERFLOW 19→1건 (잔여 1건은 별개 페이지네이션 결함, 별도 이슈 권장). 1023 lib + 6 svg_snapshot + 통합 테스트 100% 통과, 6개 다른 샘플 무회귀, 타겟 샘플 22→4 (-18) |
| #412 | 다단 우측 단 단행 문단 줄간격 누락 (vpos 보정 공식 base 차감 무력화) | 완료 | `layout.rs`: body_wide_reserved 푸시 후 y_offset 을 `col_anchor_y` 로 캡처하여 page_path 보정 anchor 로 사용. `vpos_end` 결정에 `curr_first_vpos` 우선 사용 (HWP spacing_after 인코딩 정확 반영). exam_eng p1 우측 단 item 7 ①~⑤ 22.55 px 균일, p1 좌측 단 item 1 / p2 item 20 catch-up 해결. cargo test 6/6 통과, 7개 샘플 268페이지 시각 검증 통과. 잔존 item 18 (overlay shape bypass) 별도 task 분리 권고 |
| #352 | exam_eng.hwp p5 Q32 dash 시퀀스 Justify 폭 부풀림 | 완료 | 원인: HY신명조 ASCII '-' 메트릭 0.83 em (font_metrics_data.rs:3848 FONT_276_LATIN_0[13]=853). `text_measurement.rs` `is_dash_leader_run` 헬퍼 + 3 char_width 클로저에 좁은 base 0.3 em 적용. **Stage 5**: `paragraph_layout.rs` Justify 분기에 dash leader elastic 분배 (PDF 가 dash 를 path/graphic 으로 가변 그림 — `pdftotext` 추출에서 dash 단어 부재로 확인). 라인에 ≥3 dash leader + 양수 슬랙 시, 슬랙을 dash 가 흡수 (공백 자연 폭 유지). `TextStyle::extra_dash_advance` 신규 필드. `svg.rs`/`web_canvas.rs` 가운데점 패턴 모방 글리프 스킵 + (underline 부재 시) 단일 가로선. Q32 블랭크: 12.11→7.06 px/dash 폭 351→204.7 px (PDF 218 의 94%). p6 Q33 단어 공백 자연 폭 회복 (Stage 3 의 +14~24 px 폭증 완전 해소). 1023 + 통합 테스트 전수 통과 |
