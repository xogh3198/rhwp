# 오늘 할일 - 2026년 5월 1일

## 릴리즈

| 대상 | 버전 | 상태 | 비고 |
|------|------|------|------|
| 라이브러리 (Cargo + npm + studio + vscode) | v0.7.8 → **v0.7.9** | 완료 (2026-05-01) | Task #501 + PR #428/#494/#478/#498 cherry-pick 누적. main merge (PR #504) + tag + GitHub Release. Publish All Packages workflow success — npm @rhwp/core 0.7.9 + @rhwp/editor 0.7.9 + GitHub Pages + VS Code Marketplace + Open VSX 5곳 자동 배포 완료. cargo test --lib 1102 passed, WASM 4,202,430 bytes. README 기여자 누적 갱신 (cskwork/DanMeon/oksure/planet6897/seanshin/seo-rii/dyjung150605/ggoban) |
| **rhwp-chrome / Edge** | v0.2.1 → **v0.2.2** | 제출 완료 (2026-05-01, 심사 대기) | 라이브러리 코어 v0.7.3 → v0.7.9 (4 사이클) 갱신. 4 파일 버전 동기화 (manifest + package + dev-tools-inject + content-script — v0.2.0 hotfix 사례 정합). Node v22.18.0 (rolldown 호환) 빌드, dist 28MB / zip 16MB. Chrome Web Store + Edge Add-ons 동일 zip 재사용. 등록 문구 한/영 (chrome-0.2.2_kor.md / chrome-0.2.2_eng.md) + Edge Reviewer Notes (edge-0.2.2_reviewer_notes.md) 작성, 자기검열 grep 통과 |
| **rhwp-firefox** | v0.2.1 → **v0.2.2** | AMO 제출 완료 (2026-05-01) | manifest + package 0.2.2 (Chrome 의 4 파일 동기화 함정과 다른 정합 — content-script.js 가 manifest.version 동적 참조). build.mjs dereference (rhwp-shared/sw 실체화). dist 19MB / zip 12.9MB / source zip 47.9MB (rhwp-source-0fb3e67.zip). web-ext lint: errors 0 / warnings 2 (wasm-bindgen 의 Function constructor — v0.2.1 동일 패턴). AMO 4 함정 모두 사전 점검 통과 (data_collection_permissions / gecko_android 생략 / id suffix / source zip) |

## PR 처리

| Issue | 타스크 | 상태 | 비고 |
|------|--------|------|------|
| PR #498 | Canvas visual diff 파이프라인 (@seo-rii) | 완료 (cherry-pick 머지) | 본 사이클 16번째 PR. P2 (PR #456 PageLayerTree replay 전환) 후속 P3 검증 레이어. **rhwp-studio E2E + GitHub Actions Render Diff workflow** 추가 — legacy Canvas (PageRenderTree) ↔ layer Canvas (PageLayerTree replay) 픽셀 diff 자동 검증. 7 commits 분리 cherry-pick (test + diagnostics + docs + CI runner + 보안 hardening 3건). 변경 영역: JS E2E + CI workflow + Vite 설정 + 문서 (Rust 변경 0). 작성자 보고: 기본 fixture 3개 (KTX/biz_plan/tac-case-001) 모두 0 diff. 검증: cargo test --lib 1102 passed, svg_snapshot 6/6, issue_418 1/1, issue_501 PASS, clippy 0건 (Rust 영역 영향 없음). 본 PR 머지 후 `Canvas visual diff` CI job 자동 실행. **작업지시자 정책**: \"메인테이너가 ios/devel 처럼 skia 쪽 렌더러도 별도 브랜치로 생성해서 위험도를 낮추는 방법을 고려 중\" — 본 검증 인프라가 향후 별도 backend (Skia/CanvasKit/ThorVG) 실험 시 backend 별 회귀 검출 도구로 활용 가능. relates #364 (CLOSED) |
| PR #478 | Task #476 + #479 + #480 + #483 + #488 + #489 + #490 + #492 + #495 합본 (@planet6897) | 완료 (cherry-pick 머지 + 후속 4차/5차) | 본 사이클 15번째 PR. 97 commits 중 페이지 레이아웃 영향이 작은 **7 Task / 10 commits** 분리 cherry-pick (PR close 후 4차/5차 후속 머지 포함). **포함**: #488 + #490 + #483 + #489 + #495 (1~3차 머지) → close → **#480 + #476 (4~5차 후속)**. **제외**: #479 (한컴 2020 시각 판정 필수, 회귀쪽 실패 — **이슈 #503** 분리), #492 (orders 갱신만, skip). 검증: cargo test --lib 1086 → 1102 passed, svg_snapshot 6/6, issue_418 1/1, issue_501 PASS (Task #501 회귀 0), clippy 0건, WASM 4,202,430 bytes (5차 후). **잔존 결함**: #495 부분 정정 (이슈 #502 — 문단 내 글상자 TextRun) + #479 미흡수 (이슈 #503 — 한컴 2020 정답지 시각 판정 필수). **작업지시자 통찰**: (1) \"문단내 글상자의 textRun 처리 문제\" — Task #495 가드는 TextRun 중복만 차단; (2) \"#479 는 회귀쪽에서 실패가 납니다. 이건 따로 분리해서 한컴 2020 버전으로 시각판정을 해야 합니다\" — typeset.rs core 영역, 광범위 paragraph 누적 영향 |

## 신규 전략 (승인 대기)

| 구분 | 타스크 | 상태 | 비고 |
|------|--------|------|------|
| 전략 | all-in-one-parser 390 fixture 1:1 시각 정합화 | 전략 작성 완료 → 승인 대기 | 작업지시자 요청. `mydocs/tech/all_in_one_parser_fidelity_strategy.md` 작성. 5 Phase (Baseline → Diff → Cluster → Fix → Gate), 6주 일정, KPI L2 SSIM ≥ 0.97. 결정 포인트 8건(D1~D8) 승인 + umbrella Issue 채번 요청. P0 후보: EQALIGN 구현(B-003), 글머리 탭 정렬(#340). 위험: 한컴 한글 2020 라이선스, 비공개 폰트, layout.rs 4.5/10 |

## 신규 정정 (완료, 커밋 대기)

| Issue | 타스크 | 상태 | 비고 |
|------|--------|------|------|
| #505 | CASES+EQALIGN 중첩 토폴로지 SVG y-scale 1.64x 글리프 왜곡 (#175 후속) | Stage 4/4 완료 → 커밋 승인 대기 | 진단으로 발견 — `D:/PARA/Resource/all-in-one-parser/input/미적분/미적분 기출문제_03.미분계수와 도함수1-1.hwp` p5 의 `g(x)= {cases{{1} over {2} x ^{2} ...}}` 분수 squashed (`12r²` 시각 결함). **본질**: `parse_command` 의 OVER/ATOP 폐기로 `parse_cases`/`parse_pile`/`parse_eqalign` 의 row-collecting 루프에서 분수 분실. **정정**: `try_consume_infix_over_atop` 헬퍼 추출 → 5개 호출지점 통합 (DRY) + tokenizer `skip_spaces` 에 `\n`/`\r` 추가. 2 파일 변경 + 신규 회귀 테스트 4 (`tests/issue_505.rs`). **결과**: pi=165 scale_y 1.64 → 1.08 (수락 기준 ≤ 1.20). 페이지 5 35 수식 중 극단 scale 그룹 1→0건. cargo test --lib 1102 통과 + clippy 0건. svg_snapshot 5/6 실패는 사전 CRLF/LF 회귀 (main 동일, 본 정정 무관). 계획서 + 단계별 보고서 + 최종 보고서 + before/after 스크린샷 비교 포함. **시각 검증**: `g(x) = | 12r² | x` → `g(x) = | (1/2) x² | x` |

## 회귀 정정

| Issue | 타스크 | 상태 | 비고 |
|------|--------|------|------|
| #501 | mel-001.hwp 2쪽 s0:pi=22 표 셀 높이 처리 회귀 (v0.7.8 + 현재 devel, 크롬 확장 배포본 정상) | 완료 (Stage 5/5) | **본질**: HWP 의 셀 IR 에 `cell.padding.top + bottom > cell.height` 인 비정상 케이스 (mel-001 셀[21] r=2 c=2 "현 원": pad=(141,141,1700,1700), cell.h=1280 HU). HWPX `hasMargin="0"` 명시 정합. **회귀 origin**: Task #347 의 `prefer_cell_axis` 가드 (`aim=false 에서도 cell > table 이면 cell 우선`) 가 mel-001 의 1700 HU 도 cell 우선 적용 → padding 합산이 cell.height 의 2.66배 → row_heights 거대 → TAC 표 비례 축소 (scale 0.45) → 모든 행 12~20px 축소 + 셀 진입 결함. **정정**: `resolve_cell_padding` 끝에 **한컴 자체 방어 로직 모방** 가드 추가 — pad_top + pad_bottom > cell.height 면 cell.height 의 절반까지 비례 축소. measure_table_impl 1-b단계도 동일 가드 (안전망). 2 파일 변경, 신규 통합 테스트 1 (issue_501.rs). cargo test --lib 1086 passed + svg_snapshot 5/6 (form_002 부동소수 정밀도 미세 차이) + issue_418 1/1 + clippy 0건 + WASM 4,206,487 bytes. **작업지시자 시각 검증** — mel-001 p2 pi=22 r=0=26.4 (정상), 합계 r=1/r=2=17.07/17.07 (균등), 셀[21] "현 원" 정상 표시 ★. 트러블슈팅 + 위키 ([HWP 셀 Padding 방어 로직](https://github.com/edwardkim/rhwp/wiki/HWP-%EC%85%80-Padding-%EB%B0%A9%EC%96%B4-%EB%A1%9C%EC%A7%81)) 작성. **작업지시자 통찰**: *"이런 경우 한컴은 자체 방어로직으로 처리한다면?"* — Task #347 가드 (KTX 목차 정합) 보존 + 한컴 동작 모방 가드 추가 |

## M100 — v1.0.0 조판 엔진

| Issue | 타스크 | 상태 | 비고 |
|------|--------|------|------|
| #508 | PageLayerTree image paint op JSON 에 brightness/contrast 필드 추가 | 완료 | 완료: 15:29, `src/paint/json.rs` serialization + test 보강. `cargo test --lib` 1102 passed |
