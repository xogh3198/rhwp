# 오늘 할일 - 2026년 5월 2일

## M100 — v1.0.0 조판 엔진 (착수)

| Issue | 타스크 | 상태 | 비고 |
|------|--------|------|------|
| **M100 #514** | 복학원서.hwp 고려대학교 학교 로고 (PCX 포맷) 출력 누락 | **완료 (Stage 5/5 + Stage 6 머지)** | 본질: 학교 로고가 PCX 포맷 (`BIN0001.PCX`, ZSoft Paintbrush v3.0+, 1980년대 비표준) 으로 임베드 → `detect_image_mime_type` 가 PCX 시그니처 (0x0A 0x05) 미인식 → `application/octet-stream` 으로 emit → 브라우저 렌더링 불가. **정정**: Cargo.toml `pcx = "0.2"` 추가 + svg.rs 와 web_canvas.rs 양쪽의 `detect_image_mime_type` 에 PCX 분기 + `pcx_bytes_to_png_bytes` 변환 함수 (paletted/RGB 모두, **흰색 → 투명 RGBA PNG**, 작업지시자 요구) + 3개 emit 사이트 (svg.rs paragraph_layout / svg.rs draw_image / web_canvas.rs draw_image) PCX → PNG 변환. **검증**: cargo test --lib 1110 + svg_snapshot 6/6 + issue_418/501/514 + paint::json 4/4 + clippy 0 + WASM 빌드 +75,918 bytes. **시각 판정**: CLI SVG (`data:image/png;base64,iVBORw0KGgo...`, RGBA color type 6) ★ + Web Canvas (작업지시자 검증: "이제 PCX 이미지가 웹 캔바스에도 출력됩니다.") ★. **구조적 사실 발견**: rhwp 의 image MIME 감지/변환 코드는 renderer 마다 별도 사본 (svg.rs / web_canvas.rs / paint/json.rs 등) — 시각 결함 정정 시 모든 경로 점검 필요. samples sweep 결과: PCX 1, WMF 7, EMF 2, OLE 3 — 본 task 는 PCX 한정 |
| **M100 #516** | rhwp-studio (web Canvas) 그림 워터마크 효과 (effect + brightness + contrast) 미적용 + AI 메타정보 추가 + 다층 레이어 인프라 도입 | **완료 (Stage 6/6 머지)** | 본질: 복학원서.hwp 가운데 고려대학교 엠블렘에 워터마크 효과 (`effect=GrayScale + brightness=-50 + contrast=70`) — Web Canvas 미적용. **본 사이클 확장**: ① ImageAttr 헬퍼 (`is_watermark` / `is_hancom_watermark_preset` / `watermark_preset`) — 한컴 자동 프리셋 (b=70 c=-50) vs 복학원서 편집자 정의 (b=-50 c=70) 분류, ② dump 4 사이트 `[image_attr]` 메타 출력, ③ PageLayerTree JSON 에 `watermark.preset` + `wrap` + `mime` 필드, ④ Web Canvas `compose_image_filter` + CSS filter 적용, ⑤ **다층 레이어 도입 (옵션 C HTML Hybrid)** — Discussion #529 후보 3 안 (Multi-Canvas / WebGPU / HTML Hybrid) 비교 후 옵션 C 확정. M200 후보 B (WebGPU) 단계적 경로 보존 (DTP 정체성), ⑥ `LayerFilter` enum + `renderPageToCanvasFiltered(layer_kind)` API + page-renderer overlay 인프라 (`applyOverlays` / `getOverlayImages` / `createOverlayLayer`) + PCX→PNG 변환 (overlay 데이터 URL), ⑦ hit-test 옵션 3-C (`findPictureAtClick` 두 단계 — 텍스트 우선 / BehindText 후순위), ⑧ 회귀 테스트 `tests/issue_516.rs` 8 tests. **검증**: cargo test --lib 1110 + svg_snapshot 6/6 + issue_418/501/514/516 + clippy 0 + tsc 0 + WASM 빌드 4.45MB. **작업지시자 시각 판정**: 학교 로고 ✅ + hit-test 옵션 3-C ✅ + 부수 (그림 객체 선택+드래그) ✅. **분리 task**: 워터마크 회색조/투명도 시각 정합 → 이슈 #535 ("한컴을 믿으면 안 됨, 자체 D-1 해석"). **DTP 정체성 통찰**: 아래아한글 = 쿽 익스프레스 대체 의도 → 다층 레이어 = DTP 인프라. **권위 메모리 후보**: rhwp 자체 시각 해석 권위 (한컴 출력 권위 미입증, `feedback_pdf_not_authoritative` 정합) |

## 회귀 정정

| Issue | 타스크 | 상태 | 비고 |
|------|--------|------|------|
| **M100 #530** | treatise sample.hwp 5페이지 TAC 표 Top caption 머리행 겹침 정정 | **완료 (Stage 5/5)** | 최종 보고 완료: 15:50, 이슈 close 승인 대기 |
| **M100 #509** | PUA (Private Use Area) 글머리표 글리프 회귀 — KTX/synam-001/kps-ai 등 14 샘플의 글머리표가 ☰ (폰트 폴백 결함) 또는 잘못된 글리프로 출력 | **완료 (Stage 6/6)** | **본질**: PR #251 (외부 컨트리뷰터, 2026-04 영역) 의 일반 텍스트 PUA 변환 도입 후 매핑 표 정확성 영역 회귀. **진단 영역**: Stage 1 — 광범위 PUA 사용 영역 정합 (14 샘플 / 18 codepoint / 111 회). Stage 2 — `mydocs/manual/hwp/Help/extracted/hwpbase/hncpuaconverter.htm` 정독, KS X 1026-1:2007 표준 정합 (별도 task #512 등록). Stage 3~4 — 4개 옵션 (A/B/C/F) 점검, **Option F 확정** (PR #251 draw_text 영역 보존 + 매핑 표 한컴 PDF 정답지 정확화). **작업지시자 통찰**: *"한컴의 경우 PUA 는 폰트지정과 상관없이 처리해야 할 것 같습니다. 지정된 폰트에 없는 경우 현재 문제가 되는 것으로 판단됩니다."* → rhwp 도 한컴과 동일하게 PUA 자체 변환 발행. **검증 도구**: `gen-pua` 서브명령 추가 — 18 종 PUA 코드포인트 영역 검증용 HWP 생성. samples/pua-test.hwp + 한컴 2022 출력 PDF 캡처 → 정답지 영역 정합. **정정 매핑 (2개)**: U+0F0A0 ▪ U+25AA → · U+00B7 (Middle dot, synam-001 9회), U+0F0E8 ➤ U+27A4 → ➔ U+2794 (Heavy wide-headed arrow, kps-ai 4회). **신규 매핑 (10개)**: Supplementary PUA-A (U+F02B0~F02FF) — U+F02B1~B9 → ① ~ ⑨ (mel-001/kps-ai), U+F02EF → · (KTX). **검증 게이트**: cargo build (lib) + pua_mapping 5/5 + svg_snapshot 6/6 회귀 0 + WASM 4.18MB 빌드 + rhwp-studio 배포. **작업지시자 시각 검증** — KTX p10 + samples/pua-test.hwp 정상 출력 ★. **별도 결함 (본 task 너머)**: Supplementary PUA-A 의 SVG 출력 누락 — 함수 매핑 정확하나 SVG 렌더링 영역에서 누락 (UTF-16 surrogate pair 처리 결함 추정), 별도 task 분리 권장 |

## 외부 PR 처리

| PR | 제목 / 작성자 | 상태 | 비고 |
|----|--------------|------|------|
| **#507** | CASES+EQALIGN 중첩 토폴로지 분수 분실 정정 (#505 / #175 후속) — @cskwork (Agentic-Worker) | **수정 요청 (open)** | 코드 변경 (헬퍼 추출 + DRY) 과 회귀 테스트 9건 (인라인 fixture 기반) 자체는 정합. 그러나 본질이 시각 결함 정정 (`12r²` squashing → `(1/2) x²`) 이고 `samples/` 에 `미적분 기출문제_03.미분계수와 도함수1-1.hwp` 부재. 메모리 `feedback_v076_regression_origin` / `feedback_pdf_not_authoritative` / `reference_authoritative_hancom` 정합으로 작업지시자 직접 시각 판정 게이트 미충족 → 한컴 2010+2020 직접 판정 필요. PR comment 등록 — `samples/` 추가 요청 (라이선스 절차 불필요). 문서: `mydocs/pr/pr_507_review.md` |
| **#510** | Task #508: PageLayerTree image brightness/contrast JSON 필드 추가 — @postmelee (Taegyu Lee), alhangeul-macos downstream | **머지 완료 (cherry-pick)** | 데이터 contract 보강 — `PaintOp::Image` JSON 에 `brightness`/`contrast` 4 줄 추가, schemaVersion 유지 (additive). 소스 1 파일 (+6/-2) + 테스트 assertion 4 줄. 단일 commit `50a9f63` cherry-pick → `548df60` (footer + `closes #508` 보강). **검증**: paint::json 4/4 + lib 1110/1110 + clippy 0 + issue_418/501/svg_snapshot 회귀 0 + **before/after SVG byte-identical (380,804 bytes)** ★. 작업지시자 시각 판정 승인. 문서: `mydocs/pr/pr_510_review.md` + `pr_510_review_impl.md` + `pr_510_report.md` |

## M100 — 옛한글 PUA 변환 (Task #528, #512 흡수)

| Issue | 타스크 | 상태 | 비고 |
|------|--------|------|------|
| **M100 #528** | 옛한글 PUA → KS X 1026-1:2007 자모 변환 + 폰트 fallback (#512 흡수) — exam_kor 17p 미렌더 | **완료 (Stage 5/5) ★** | **본질 정정 발견 (Stage 1)**: v1 가설 (Hangul Jamo Extended-A/B 폰트 fallback) 부정확. exam_kor p17 측정으로 **HanCom Hanyang-PUA 인코딩** (BMP PUA U+E0BC-F8F7) 본질 확정 → Issue #512 흡수. **자료원**: KTUG HanyangPuaTableProject (Public Domain, 5,660 매핑, 함초롬바탕 정합). **변환**: `src/renderer/pua_oldhangul.rs` (자동 생성 5,773 라인, exam_kor 25/25 100%) + Composer `display_text` 인프라 + svg.rs/web_canvas.rs `draw_text` 시점 변환 (Option A — 인덱싱 불변성 유지). **폰트**: Source Han Serif K (Adobe + Google, **SIL OFL 1.1**) Old Hangul subset (234 KB woff2, 357/368 cmap, ccmp/ljmo/vjmo/tjmo 4/4). `web/fonts/SourceHanSerifK-OldHangul-subset.woff2` + `SourceHanSerifK-OFL.txt`. CSS unicode-range 격리 (일반 한글 미영향). font-family 체인 보강 (mod.rs::generic_fallback 3 위치). **Stage 4 hotfix**: 책괄호 (U+F0854/F0855 → 《 》, 33+33회) + 예시 마커 (U+F00DA → ▸, 2회 잠정) 매핑 추가 (paragraph_layout.rs::map_pua_bullet_char Supp PUA-A 핸들러 확장). **검증**: cargo test --lib 1116 + svg_snapshot 6/6 (golden 갱신 cosmetic — font-family chain 추가만) + issue_418/501 + exam_kor p17 PUA 잔존 **0** (이전 112 → 0). **작업지시자 시각 판정 ★**. 보고서: `mydocs/report/task_m100_528_report.md`. 자료원 문서: `mydocs/tech/pua_oldhangul_mapping_sources.md`. font_fallback_strategy.md '10. 옛한글 fallback' 섹션 추가 |

## M100 — exam_kor 14p Square wrap 표 직후 baseline 누락 (Task #533)

| Issue | 타스크 | 상태 | 비고 |
|------|--------|------|------|
| **M100 #534 v2** | exam_kor.hwp 18p Square wrap 표 + TAC 그림 위치 — v1 fix (inner_pad) 후 PDF 시각 비교로 잔존 결함 발견, LINE_SEG.column_start 정합 추가 fix | **v2 Stage 완료, close 진행** | v1 fix 후 PDF 시각 검증: picture 가 [A] 박스 위에 겹쳐 표시 (column 시작 영역, 표 옆에 정상 배치 안 됨). 본질 정정: HWP IR `LINE_SEG.column_start` (cs=2855 HU = 38.07 px for pi=50) 가 Square wrap 표 + outer_margin 이후 텍스트 시작 위치를 인코딩. layout_shape_item 가 cs 미반영. 정정: `cs > effective_margin_left` 시 cs 우선 (max 패턴, +12 라인). picture x: 604.72 → **620.12** ✓ PDF 시각 정합. 검증: lib 1116 + svg_snapshot 6 + issue_418/501 + 광범위 192 페이지 / 2 변경 (의도). |

| **M100 #534 v1** | exam_kor.hwp 18p — Square wrap 표 + tac=true 그림 그림 x 좌표 inner_pad 누락 (1차 부분 fix, v2 로 보강) | **v2 흡수** | 본질: layout_shape_item 의 TAC Picture x 계산이 paragraph_layout 의 inner_pad 로직 (visible stroke + border_spacing[0,1]=0 케이스) 과 비정합. wrap_host paragraph 는 paragraph_layout 미호출되어 layout_shape_item 만 emit → inner_pad 누락. 정정: layout.rs::layout_shape_item TAC 분기에 has_visible_stroke + border_spacing 검사 + inner_pad_left 가산 (+19 라인). Stage 1 추가 조사 (eprintln) 로 emit code path 식별 후 본질 확정. 검증: lib 1116 / svg_snapshot 6 / issue_418/501 / 광범위 8 샘플 192 페이지 중 190 byte-identical / 변경 2 페이지 (p18 pi=50/56 + p19 pi=60) 모두 동일 본질 의도된 정정. p18 image x 593.39 → 604.72 (+11.33 px), p19 128.51 → 139.84 (+11.33 px) 정합. |

| **M100 #533** | exam_kor.hwp 14p 우측 단 — Square wrap 표 다음 문단 baseline 누락 (~13px 시프트) | **Stage 4/5 완료, 시각 판정 대기** | 본질: 비-TAC Square wrap 표 직후 y_offset 이 표 bottom 으로만 advance → 호스트 텍스트 > 표 영역 케이스에서 다음 문단 baseline 누락 (~13px 시프트). 정정: `src/renderer/layout.rs::layout_table_item` 에 `host_text_bottom = para_y + (last.vpos+lh - first.vpos)/dpi` max 가산 (+18 라인). vpos 가 column 누적 좌표 → first.vpos 차감 패턴 필수 (Stage 3 1차 시도 결함 정정). 검증: cargo test --lib 1116 + svg_snapshot 6/6 + issue_418/501 회귀 0. 광범위 8 샘플 192 페이지 중 190 byte-identical, 변경 2 페이지 (exam_kor p14 + p17) 모두 동일 본질 의도된 정정 (p14 col 0/1 5 곳 + p17 pi=2 +12.59px). Stage 5 시각 판정 영역: `/tmp/p14_final/exam_kor_014.svg` + `/tmp/p17_final/exam_kor_017.svg` |

## 본 세션 요약

- planet6897 작성 6건 모두 close (재현 불가 1, 보류 3, 해결 1, 신규 fix 1)
- Layout 리팩터링 Phase 0/1/2 완료 — devel 푸시 9회
- 신규 issue 등록 + close 2건 (#517 Phase 1, #518 Phase 2)
- 코드 변경: `src/renderer/layout/paragraph_layout.rs` (Phase 1 logging + Phase 2 break 다중화) + `src/renderer/layout/table_layout.rs` (#500 fix)
- 인프라 추가: `scripts/svg_regression_diff.sh` + `mydocs/manual/svg_regression_diff.md`
- 모든 변경 단위 1103 + 광범위 회귀 검증 통과
- Task #519 추가 — 그림 flip/rotation 속성 SVG 적용 (6 ImageNode 지점, exam_eng p4 28번 curl 정합)

## 작업 메모

- Task #509 정합 — `feedback_pdf_not_authoritative` 영역에 한컴 2022 편집기 영역의 직접 시각 캡처는 정답지 영역으로 활용 가능 (단, 작업지시자가 직접 캡처한 영역만)
- `gen-pua` 도구 — 향후 PUA 회귀 검증 영역의 결정적 영역 도구로 보존 (samples/pua-test.hwp 영역 영구 보존 권장)
- PR #510 처리 — core SVG byte-identical 결과는 contract 보강 PR 의 정합 검증 패턴으로 보존 가치. 향후 유사 contract-only 변경 시 동일 검증 적용
