# 수행 계획서 — Task M100 #416

## 이슈

[#416](https://github.com/edwardkim/rhwp/issues/416) — find_bin_data 가드 결함 (Task #195 회귀, 페이지 배경 이미지 잘못 표시)

## 목적

`src/renderer/layout/utils.rs` 의 `find_bin_data` 함수에 도입된 잘못된 가드를 제거하여 일반 HWP 그림의 인덱스 기반 매핑을 복원하면서, HWPX 차트의 sparse id 경로도 보존.

## 결함 origin

| 항목 | 값 |
|------|-----|
| 회귀 도입 커밋 | `5c72f48` (Task #195, @planet6897, 2026-04-20) |
| 결함 위치 | `src/renderer/layout/utils.rs:13-25` |
| 영향 | storage_id ≠ 인덱스인 모든 hwp 파일 (`hwpspec.hwp` 등) |
| 트러블슈팅 | `mydocs/troubleshootings/bin_data_id_index_mapping.md` (2026-02-17 동일 결함 정정 이력) |

## 결함 코드 (현재)

```rust
pub(crate) fn find_bin_data<'a>(bin_data_content: &'a [BinDataContent], bin_data_id: u16) -> Option<&'a BinDataContent> {
    if bin_data_id == 0 {
        return None;
    }
    // 1-indexed 순번으로 먼저 조회 (기존 동작 유지)
    if let Some(c) = bin_data_content.get((bin_data_id - 1) as usize) {
        if c.id == bin_data_id {            // ← 결함: c.id 는 storage_id, bin_data_id 는 인덱스
            return Some(c);
        }
    }
    // 실패 시 id 필드로 직접 검색 (HWPX 차트처럼 sparse id 사용 시)
    bin_data_content.iter().find(|c| c.id == bin_data_id)   // ← 트러블슈팅 정정 이력의 결함 패턴
}
```

## 정정 코드 (옵션 B)

```rust
pub(crate) fn find_bin_data<'a>(bin_data_content: &'a [BinDataContent], bin_data_id: u16) -> Option<&'a BinDataContent> {
    if bin_data_id == 0 {
        return None;
    }
    // 1-indexed 순번으로 BinDataContent 배열 접근 (storage_id 가드 제거)
    if let Some(c) = bin_data_content.get((bin_data_id - 1) as usize) {
        return Some(c);
    }
    // 인덱스 범위 밖 (HWPX 차트 sparse id 60000+N 등) — id 직접 검색
    bin_data_content.iter().find(|c| c.id == bin_data_id)
}
```

### 동작 분기

| 조건 | 매칭 방식 |
|------|----------|
| `bin_data_id == 0` | None |
| `bin_data_id ≤ bin_data_content.len()` | **인덱스 (1-indexed)** — 일반 HWP 그림 |
| `bin_data_id > bin_data_content.len()` (HWPX 차트, id=60000+N 등) | **id 직접 검색** — sparse id 경로 |

## 변경 범위

`src/renderer/layout/utils.rs` 단일 파일, `find_bin_data` 함수 본체만 수정.

10+ 호출 지점은 모두 본 헬퍼 경유이므로 자동 적용:
- `renderer/layout.rs` (페이지 배경, 일반 그림 등)
- `renderer/layout/table_layout.rs`, `table_cell_content.rs`, `picture_footnote.rs`, `shape_layout.rs`, `paragraph_layout.rs`

## 검증 방법

### 자동 검증

| 검증 | 명령 |
|------|------|
| cargo lib test | `cargo test --lib` (1016+ passed 무회귀) |
| svg_snapshot | `cargo test --test svg_snapshot` (6/6) |
| clippy | `cargo clippy --lib -- -D warnings` |
| **신규 회귀 테스트** | hwpspec.hwp 의 페이지 배경 이미지가 정확한 BinData 와 매칭되는지 단위 테스트 추가 |

### 시각 / 수동 검증

| 시나리오 | 기대 |
|----------|------|
| `samples/hwpspec.hwp` 1 페이지 export-svg --debug-overlay | 페이지 표지 이미지 정상 (159×247.6mm PNG, 1137 bytes) |
| 다른 일반 hwp 샘플 (k-water-rfp, exam_eng 등) | 무회귀 |
| HWPX 차트 샘플 | 차트 정상 (sparse id 경로) |
| 작업지시자 시각 판정 | 통과 |

### HWPX 차트 샘플 식별

`samples/hwpx/` 또는 `samples/` 의 차트 포함 hwpx 파일 검색하여 차트 렌더 회귀 점검.

## 단계 분할 (구현 계획서 후속)

3 단계 예상:

1. **Stage 1**: `find_bin_data` 함수 정정 + 단위 테스트 추가 (sparse id / 인덱스 매칭 분기 검증)
2. **Stage 2**: 회귀 테스트 — `hwpspec.hwp` 1 페이지 SVG export 시 페이지 배경 매칭 검증, HWPX 차트 샘플 회귀 점검
3. **Stage 3**: 자동 검증 (`cargo test --lib`, svg_snapshot, clippy) + 시각 검증 + 트러블슈팅 문서 갱신

상세 단계는 구현 계획서에서 확정.

## 위험 / 주의

### 1. HWPX 차트 회귀

Task #195 의 차트 sparse id 처리가 영향 받지 않는지 점검. 옵션 B 의 분기 (인덱스 범위 안 → 인덱스, 밖 → id) 가 정확히 양쪽을 커버하는지 검증 필요.

### 2. 다른 hwp 샘플의 회귀

대부분 storage_id 가 인덱스와 일치하여 가드 통과해 왔던 샘플들 — 인덱스 매칭으로 변경 시 회귀 가능성은 낮으나 svg_snapshot 등으로 점검.

### 3. 트러블슈팅 문서 갱신

본 회귀의 도입 → 정정 이력을 명시. 향후 비슷한 fallback 추가 시 가드 추가에 신중하도록 교훈 명시.

## 산출물

| 단계 | 산출물 |
|------|--------|
| Stage 1 | `mydocs/working/task_m100_416_stage1.md` |
| Stage 2 | `mydocs/working/task_m100_416_stage2.md` |
| Stage 3 | `mydocs/working/task_m100_416_stage3.md` |
| 최종 | `mydocs/report/task_m100_416_report.md` |
| 트러블슈팅 갱신 | `mydocs/troubleshootings/bin_data_id_index_mapping.md` (이력 추가) |

## 다음 단계

본 수행 계획서 승인 → 구현 계획서 작성.
