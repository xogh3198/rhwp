# 수행 계획서 — Task M100 #418

## 이슈

[#418](https://github.com/edwardkim/rhwp/issues/418) — `samples/hwpspec.hwp` 20 페이지 이미지 이중 출력 (PR 처리 후 회귀)

## 결함 origin — Task #376 머지 누락

### 정황

| 항목 | 값 |
|------|------|
| 결함 정정 commit | [`45419a2`](https://github.com/edwardkim/rhwp/commit/45419a2) — "Task #376: 빈 문단 + TAC Picture 의 paragraph_layout / layout_shape_item 이중 emit 제거" (2026-04-27, @planet6897) |
| 이슈 #376 상태 | **CLOSED** (2026-04-27) — 이슈 종료 |
| commit 분기 포함 여부 | `pr-360-head` 임시 브랜치에만 존재. **devel / main 미머지** |
| PR | 없음 — gh pr list 검색 결과 0건 |

이슈가 close 됐지만 정정 commit 이 devel 에 머지되지 않은 정황. 메인테이너의 머지 누락.

### 현재 코드 결함 (Task #376 정정 누락 상태)

**1. `src/renderer/layout/paragraph_layout.rs:2030-2046`**

빈 문단 + TAC Picture 분기에서 `line_node.children.push(img_node)` 후 **`set_inline_shape_position` 호출 누락**:

```rust
// 현재
let img_node = RenderNode::new(...);
line_node.children.push(img_node);
img_x += tac_w;
// ← 여기에 set_inline_shape_position 호출이 빠짐
```

같은 분기의 Equation 처리 (line 2133) 는 `set_inline_shape_position` 호출함 — Picture 분기만 누락.

**2. `src/renderer/layout.rs:2554-2596`**

`layout_shape_item` 의 Task #347 분기가 `if !has_real_text` 만 보고 ImageNode 를 push:

```rust
// 현재
if !has_real_text {
    // ImageNode 생성 + push
    col_node.children.push(img_node);  // ← 이미 등록됐는지 검사 없음
    tree.set_inline_shape_position(...);
}
```

`get_inline_shape_position` 검사가 없어 이미 paragraph_layout 에서 emit 된 경우에도 또 push.

### 결과

빈 문단 + TAC Picture 케이스에서 **두 경로 모두에서 emit** → 이중 출력.

`samples/hwpspec.hwp` 20 페이지 (s2:pi=83/86/89, 모두 빈 문단 + TAC=true Picture) 에서:
- y 좌표 약 2.67px 차이로 같은 이미지 두 번 그려짐 (3 쌍, 6 image 요소)

## 정정 방안 — Task #376 의 코드를 정확히 재적용

### 변경 1 — `paragraph_layout.rs:2042` 다음

```rust
line_node.children.push(img_node);
+ // [Task #418/#376] layout_shape_item 의 Task #347 분기 (빈 문단 + TAC Picture 직접 emit)
+ // 와 이중 렌더링되지 않도록 인라인 위치를 등록한다. layout_shape_item 은 등록된 경우 스킵한다.
+ tree.set_inline_shape_position(
+     section_index, para_index, tac_ci, img_x, img_y,
+ );
img_x += tac_w;
```

### 변경 2 — `layout.rs:2554` 분기에 가드 추가

```rust
let has_real_text = para.text.chars()
    .any(|c| c > '\u{001F}' && c != '\u{FFFC}');
+ // [Task #418/#376] paragraph_layout 의 빈 문단 + TAC Picture 분기에서 이미 ImageNode 가
+ // emit 된 경우 inline_shape_position 이 등록되어 있다. 등록된 경우 ImageNode push 를
+ // 스킵하고 result_y 만 갱신하여 이중 emit 을 방지한다.
+ let already_registered = tree.get_inline_shape_position(
+     page_content.section_index, para_index, control_index,
+ ).is_some();
- if !has_real_text {
+ if !has_real_text && !already_registered {
    // ImageNode 생성 + push
    col_node.children.push(img_node);
    tree.set_inline_shape_position(...);
    result_y = pic_y + pic_h;
}
+ else if !has_real_text && already_registered {
+     // 이미 paragraph_layout 에서 emit 됨 — result_y 만 갱신
+     result_y = pic_y + pic_h;
+ }
```

## 변경 범위

`src/renderer/layout/paragraph_layout.rs` + `src/renderer/layout.rs` — 2 파일.

## 검증 방법

### 자동 검증

| 항목 | 명령 |
|------|------|
| cargo lib test | `cargo test --lib` (1023+ 무회귀) |
| svg_snapshot | `cargo test --test svg_snapshot` (6/6) |
| clippy | `cargo clippy --lib -- -D warnings` |
| **신규 회귀 테스트** | hwpspec.hwp 20 페이지 \<image\> 6 → 3 으로 감소 검증 |

### 시각 / 수동 검증

| 시나리오 | 기대 |
|----------|------|
| `samples/hwpspec.hwp` 20 페이지 export-svg | `<image>` 6 → 3 (3 쌍의 이중 출력 해소) |
| 페이지 표지 (Task #416 효과 보존) | 정상 |
| 다른 hwp 샘플 (k-water-rfp, exam_eng 등) | 무회귀 |
| 작업지시자 시각 판정 | 통과 |

## 단계 분할 (구현 계획서 후속)

3 단계 예상:

1. **Stage 1**: `paragraph_layout.rs` 의 Picture 분기에 `set_inline_shape_position` 추가
2. **Stage 2**: `layout.rs` 의 layout_shape_item 분기에 `get_inline_shape_position` 가드 추가
3. **Stage 3**: 회귀 테스트 + 자동 검증 + 시각 검증 + WASM 빌드

## 위험 / 주의

### 1. Task #376 정정의 누락 영역

Task #376 commit (`45419a2`) 의 변경 범위가 정확히 본 task 정정과 같음 — 그 commit 의 diff 를 그대로 재적용. cherry-pick 도 가능하나, 본 결함이 명확하므로 직접 정정 + commit 으로 진행.

### 2. `get_inline_shape_position` 정확성

paragraph_layout 에서 `set_inline_shape_position` 호출 시점이 layout_shape_item 호출보다 먼저인지 점검. 호출 순서는 `layout_paragraph_with_layout` → `layout_shape_item` 순서이므로 paragraph_layout 가 먼저 등록 → layout_shape_item 가 검사 → 정합.

### 3. 다른 케이스 회귀

- inline TAC Picture 가 있는 일반 문단 (텍스트 + TAC) — `has_real_text == true` 라 layout_shape_item 의 `if !has_real_text` 분기 진입 안 함 → 영향 없음
- TAC=false Picture — paragraph_layout 의 본 분기 진입 안 함 → 영향 없음
- Equation TAC — 이미 `set_inline_shape_position` 호출 중 (line 2133) → 영향 없음

## 산출물

| 단계 | 산출물 |
|------|--------|
| Stage 1 | `mydocs/working/task_m100_418_stage1.md` |
| Stage 2 | `mydocs/working/task_m100_418_stage2.md` |
| Stage 3 | `mydocs/working/task_m100_418_stage3.md` |
| 최종 | `mydocs/report/task_m100_418_report.md` |

## 다음 단계

본 수행 계획서 승인 → 구현 계획서 작성.

## 참고

- 결함 origin commit: [`45419a2`](https://github.com/edwardkim/rhwp/commit/45419a2)
- 관련 이슈: [#376](https://github.com/edwardkim/rhwp/issues/376) (CLOSED, 머지 누락 정황)
- 관련 task: #347 (TAC 그림 z-order), #287 (빈 runs TAC 수식 인라인)
