# Task #431 수행 계획서 — synam-001.hwp 15페이지 분할 표 셀 내 문단 미출력

## 배경

[이슈 #431](https://github.com/edwardkim/rhwp/issues/431) — `samples/synam-001.hwp` 15페이지 (인덱스 14) 가 빈 채로 출력. 페이지 14, 15, 16 에 동일 fragment `PartialTable rows=6..7 cont=true` 가 반복.

**작업지시자 통찰** (결정적):
- VSCode 확장 (4/13 빌드 WASM): **정상 조판** ✓
- 크롬 확장 (4/21 빌드 WASM): **정상 조판** ✓
- rhwp-studio + cli (4/30 현재): **결함** — 빈 페이지 + fragment 반복

## 결함 본질

표 pi=140 (8행 × 2열, RowBreak 정책) 의 **row 6** (셀[13] r=6, c=1) 가 paras=85 (85개 문단) + h=239,230 HU (= 844.7 mm, 페이지 본문 높이의 ~2.5배). 

분할 표 처리에서:
- **VSCode/Chrome (4/13~4/21 빌드)**: 셀 내 문단을 페이지 경계에서 분할 → 각 페이지에 일부 paras 출력 (정상)
- **현재 (4/30)**: row 6 fragment 가 매 페이지마다 빈 채로 (`used=0.0px`) 반복 출현 → **셀 내 문단 분할 회귀**

## 회귀 origin 확정 (bisect)

| 항목 | 정합 |
|------|------|
| **commit** | **`af6753f5`** "Task #362: kps-ai PartialTable + Square wrap 처리 (8항목 누적 수정)" |
| **머지 시점** | 2026-04-27 13:10 (메인테이너 직접 정정) |
| **Task** | **#362** (closed) |

## Task #362 의 변경 영역

| 파일 | 변경 |
|------|------|
| `src/renderer/typeset.rs` | TypesetState wrap_around_cs/sw/table_para 추가, wrap-around 메커니즘 이식, vpos-reset 가드, hide_empty_line, 빈 paragraph skip 가드 강화 |
| `src/renderer/layout/table_layout.rs` | 외부 셀 vpos 가드, **compute_cell_line_ranges 의 한 페이지보다 큰 nested table 분할 허용** ← 본 결함과 가장 가까움, calc_visible_content_height_from_ranges_with_offset 신설 |
| `src/renderer/layout/table_partial.rs` | split_start 시 with_offset 변형 호출 |
| `src/renderer/height_measurer.rs` | **nested table 셀 capping 강화 (외부 행 높이로 cap)** ← 본 결함과 직접 연관 가능 |

## 정정 방향 (가설)

Task #362 의 **`height_measurer.rs::nested table 셀 capping`** 또는 **`table_layout.rs::compute_cell_line_ranges 한 페이지보다 큰 nested table 분할`** 이 회귀 origin 의심.

본 결함은 nested table 이 아닌 **단일 표의 row 가 페이지 폭 초과**하는 케이스이므로 — Task #362 의 nested table 영역 정정이 **단일 표의 큰 row** 케이스도 영향 줘서 셀 내 문단 분할 경로를 깬 가설.

Stage 1 (베이스라인 측정) 에서 정확한 회귀 코드 위치 식별.

## 회귀 검증 영역 (Task #362 정정 의도 보존)

Task #362 의 의도 케이스:
- kps-ai.hwp 88 → 79 페이지 (Paginator 78 와 1 차이로 거의 일치)
- p56 외부 표 안 콘텐츠 클립 차단
- p67 PartialTable nested 표 정상 표시
- p68-70 빈 페이지 2개 차단
- p72-73 pi=778 표 누락 차단

본 정정은 위 케이스들을 보존해야.

## 메모리 원칙 정합

- **`feedback_visual_regression_grows`**: dump-pages 출력은 같은데 SVG 출력이 다름 — 자동 검증 만으로는 시각 회귀 검출 불가, 작업지시자 시각 판정 게이트 필수 (본 결함의 정확한 본질 파악도 작업지시자 통찰로 가능)
- **`feedback_search_troubleshootings_first`**: 트러블슈팅 사전 검색 — `typeset_partial_table_wrap_around.md` (Task #362 의 트러블슈팅 자체) + `typeset_layout_drift_analysis.md` 정합
- **`feedback_hancom_compat_specific_over_general`**: Task #362 의 일반화 정정 (8항목 누적) 이 다른 케이스 (synam-001) 회귀 → 케이스별 명시 가드 우선

## 단계

| Stage | 내용 |
|-------|------|
| 1 | 베이스라인 측정 — 결함 정황 정확한 코드 경로 식별 + Task #362 의 어느 변경이 회귀 origin 인지 좁히기 |
| 2 | 정정 적용 — 셀 내 문단 분할 경로 복원 또는 Task #362 의 nested table 가드를 단일 표 큰 row 케이스에 적용 안 되도록 |
| 3 | 광범위 회귀 검증 — kps-ai p56/p67/p68-70/p72-73 정합 보존 + synam-001 p15 정정 + 다른 표 회귀 0 |
| 4 | 시각 검증 (작업지시자) |
| 5 | 최종 결과보고서 + 오늘할일 갱신 |

## 단계 수: 5
