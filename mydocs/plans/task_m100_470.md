# Task #470 수행계획서

## 이슈

- 번호: #470
- 제목: 21_언어_기출 1p 좌측 단 pi=10 cross-column vpos-reset 미인식 (cv != 0)
- 마일스톤: M100
- 브랜치: `local/task470`

## 배경

`samples/21_언어_기출_편집가능본.hwp` 페이지 1 좌측 단 하단 pi=10 ("적합성 검증이란…") 의 처음 2줄이 본문 영역을 56.2px 넘쳐서 표시. HWP 원본은 pi=10 전체가 우측 단 상단에 위치.

LAYOUT_OVERFLOW 메시지:
```
LAYOUT_OVERFLOW: page=0, col=0, para=10, type=PartialParagraph, y=1492.3, bottom=1436.2, overflow=56.2px
```

dump-pages: col 0 hwp_used≈159px, 실제 used=1202.7px — HWP 의도와 큰 격차.

## 원인 (분석 완료)

`src/renderer/typeset.rs:409-419` cross-paragraph vpos-reset 검출이 `cv == 0` (정확히 0) 만 인정:

```rust
if cv == 0 && pv > 5000 {
    st.advance_column_or_new_page();
}
```

본 케이스:
- pi=9 last vpos = 90426
- pi=10 first vpos = **9014** (col 1 컬럼 헤더 오프셋, 0 이 아님)

`cv < pv` (9014 < 90426) 는 명백한 컬럼 reset 이지만 `cv == 0` 가드 때문에 미감지.

## 수행 범위

1. `src/renderer/typeset.rs:415` 조건 완화: `cv == 0` → `cv < pv`
2. `src/renderer/pagination/engine.rs` 에 동일 cross-paragraph vpos-reset 검출 추가 (현재 부재 — `RHWP_USE_PAGINATOR=1` 시 동일 회귀 가능성)
3. 단위 통합 테스트 추가
4. 다단 샘플 회귀 검증

## 위험 / 부작용

- 작은 vpos 차이로 잘못된 reset 트리거 가능성 → `pv > 5000` 가드는 유지
- wrap-around 가 활성인 paragraph 는 `wrap_around_cs >= 0` 가드로 제외 (기존 로직 유지)
- 다단 / 단단 모두에서 동작 — 단단 케이스에서 vpos 가 단조 증가하지 않는 케이스가 있는지 확인 필요

## 단계 구성

3단계:
1. **Stage 1**: 신규 통합 테스트 작성 + 현행 fail 확인
2. **Stage 2**: typeset.rs 조건 완화 + pagination/engine.rs 동기 보강 + green
3. **Stage 3**: 다단 샘플 회귀 검증 + 최종 정리

## 검증

- 신규 테스트: 21_언어_기출 1p col 0 에 pi=10 PartialParagraph 가 없어야 함 / col 1 첫 항목이 pi=10 (lines 0..) 이어야 함
- LAYOUT_OVERFLOW 메시지 사라지는지 확인
- 전체 cargo test 통과
- exam_kor / exam_eng / hwpspec-w 다단 샘플 페이지 수 / overflow 건수 변동 검토
