# Task #471 수행계획서

## 이슈

- 번호: #471
- 제목: Task #468 cross-column 검출이 stroke_sig 머지와 불일치 — bf_id 비교로 partial_end 미설정
- 마일스톤: M100
- 브랜치: `local/task471`

## 배경

Task #470 적용 후 `samples/21_언어_기출_편집가능본.hwp` 페이지 1 좌측 단 (가) 박스 하단에 가로선이 그려지는 부작용 발생. PDF 는 cross-column 으로 우측 단으로 이어지므로 하단 미그려야 함.

## 원인 (분석 완료)

`src/renderer/layout.rs` paragraph border 머지 (Task #321 v6) 는 **stroke_sig** (line_type/width/color) 기준으로 머지. 머지된 그룹의 `g.0` (bf_id) 은 첫 range 의 bf_id 만 보존.

Task #468 cross-column 검출은 **bf_id** 동등 비교. 머지로 다른 bf_id 가 한 그룹이 된 경우 검출 실패.

본 케이스 디버그:
```
DBG group bf=7 first_pi=6 last_pi=9 next_bf=4   ← 4 != 7, partial_end 미설정
DBG group bf=4 first_pi=10 last_pi=12 prev_bf=4 ← 4 == 4, partial_start 정상
```

비대칭:
- col 0: bottom 그려짐 ❌
- col 1: top 미그려짐 ✓

## 수행 범위

`src/renderer/layout.rs:1670-1699` Task #468 검출 로직을 **stroke_sig** 비교로 변경.

## 단계 구성

3단계:
1. Stage 1: 통합 테스트 추가 (좌측 단 (가) 박스 하단 가로선 부재 검증) → red
2. Stage 2: stroke_sig 비교로 변경 → green
3. Stage 3: 다단 샘플 회귀 검증 + 정리

## 위험 / 부작용

- bf_id 다르지만 stroke_sig 같은 인접 paragraph 가 의도치 않게 partial 로 인식될 수 있음 → cross-column 시 발생 (현재 케이스가 그것)
- 단일 컬럼/페이지 내에서는 여전히 그룹 머지로 한 박스 — 영향 없음

## 검증

- 신규 통합 테스트: 21_언어_기출 1p col 0 영역 (x ∈ [128, 542], y ≈ 1438) 에 가로선 부재
- exam_kor / exam_eng / hwpspec 등 회귀 점검
- cargo test 전체 통과
