# Task M100 #480 수행 계획서

| 항목 | 내용 |
|------|------|
| 이슈 | [#480](https://github.com/edwardkim/rhwp/issues/480) |
| 마일스톤 | M100 (v1.0.0) |
| 브랜치 | `local/task480` |

## 1. 증상

`samples/21_언어_기출_편집가능본.hwp` 페이지 14 단 1의 paragraph 299의 표("[A]" 박스)가 단 1 본문 안이 아니라 **단 0과 단 1 사이의 갭 영역**에 그려진다.

측정값:
| 항목 | x 좌표 |
|------|--------|
| 단 1 시작 (col_area.x) | 580.16 |
| paragraph 299 본문 시작 (col_area.x + margin_left) | 602.86 ~ 602.9 (실측) |
| **현재 표 x** | **580.16** ← 단 사이 갭 |
| **기대 표 x** | **~603.6** (paragraph 영역 시작 + horz_offset) |

## 2. 근본 원인

`src/renderer/layout.rs:2256-2270` Square wrap 표 분기:

```rust
} else if !is_tac && tbl_is_square {
    let tbl_w = hwpunit_to_px(t.common.width as i32, self.dpi);
    let x = match t.common.horz_align {
        HorzAlign::Right | HorzAlign::Outside =>
            col_area.x + col_area.width - tbl_w,
        HorzAlign::Center =>
            col_area.x + (col_area.width - tbl_w) / 2.0,
        _ => col_area.x,    // ← halign=Left 시 paragraph margin 미반영
    };
    Some(x)
}
```

`halign=Left` 케이스에서 `col_area.x`만 사용하고 `effective_margin`(=margin_left + indent if indent > 0) 미반영.

`Right`/`Center` 케이스도 동일한 누락 — `col_area.width`를 그대로 사용해 paragraph margin이 빠진 단 전체 너비를 기준으로 정렬.

비교: TAC 표 분기(`layout.rs:2270-2298`)는 `effective_margin`을 정확히 적용 (`base_x = col_area.x + effective_margin + leading`).

## 3. 디버그 검증

```
[#480] Square wrap table: pi=299 col_area.x=580.16 col_area.w=425.17
       tbl_w=22.65 halign=Left effective_margin=24.59 → x=580.16
```

## 4. 수정 방향

### 옵션 A — paragraph 영역 기준으로 정렬 변경

```rust
} else if !is_tac && tbl_is_square {
    let tbl_w = hwpunit_to_px(t.common.width as i32, self.dpi);
    let area_x = col_area.x + effective_margin;
    let area_w = (col_area.width - effective_margin - margin_right).max(0.0);
    let x = match t.common.horz_align {
        HorzAlign::Right | HorzAlign::Outside => area_x + (area_w - tbl_w).max(0.0),
        HorzAlign::Center => area_x + (area_w - tbl_w).max(0.0) / 2.0,
        _ => area_x,
    };
    Some(x)
}
```

### 영향 범위

- Square wrap 표 + paragraph margin/indent 있는 케이스: x가 paragraph 영역 시작으로 이동 (수정 의도)
- margin=0 + indent=0 케이스: 변경 없음 (`area_x == col_area.x`)
- halign=Right/Center: 정렬 기준이 단 전체 → paragraph 영역으로 변경 (시각적 영향 가능, 검증 필요)

## 5. 영향 파일

| 파일 | 변경 |
|------|------|
| `src/renderer/layout.rs` | Square wrap 분기 (2256-2270 부근) |

## 6. 검증 방법

1. **핵심 회귀 케이스**: 페이지 14 [A] 박스가 paragraph 본문 영역 (x≈603)에 위치
2. **단위 테스트**: `cargo test --release`
3. **골든 SVG**: `cargo test --release --test svg_snapshot`
4. **Task #295 회귀**: halign=Right 표의 우측 정렬 동작 유지
5. **광범위 샘플 시각 검증**: Square wrap 포함 다른 샘플 (필요 시)

## 7. 단계 구성 (3단계)

| 단계 | 내용 |
|------|------|
| Stage 1 | A 옵션 적용 + 빌드 + 핵심 회귀 시각 검증 |
| Stage 2 | 단위 테스트 + 골든 SVG + 기존 Square wrap 샘플 회귀 검증 |
| Stage 3 | 디버그 로그 정리 + 최종 보고서 + 커밋 |

## 8. 리스크

| 리스크 | 완화 |
|--------|------|
| Task #295 (halign=Right)에서 회귀 | 같은 분기 처리, area_x 기반으로 동일 동작 보장 |
| paragraph margin=0인 정상 케이스 영향 | area_x = col_area.x로 동등, 영향 없음 |
| 다단/표분할 상호작용 회귀 | 골든 SVG 6건 + 기존 단위 테스트로 검출 |

자동 승인 모드로 Stage 1 진행합니다.
