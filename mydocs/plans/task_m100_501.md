# Task #501 수행 계획서 — mel-001.hwp 2쪽 s0:pi=22 표 셀 높이 처리 회귀

## 이슈 정합

- **Issue**: [#501](https://github.com/edwardkim/rhwp/issues/501) — bug, assignee = edwardkim
- **샘플**: `samples/mel-001.hwp` 2쪽 s0:pi=22 (8행×12열 인원 현황 표)
- **회귀 범위**: rhwp v0.7.8 (현재 릴리즈) + 현재 devel HEAD 모두 결함
- **정상 기준**: 크롬 확장 배포본 v0.2.1 (rhwp-chrome 5665d49, 2026-04-19)

## 본질 정합

multi-paragraph 셀 (예: 합계 행의 "정 원" + 숫자 두 줄, h=2560 HU rs=2) 의 셀 높이 계산이 부족하여 셀 콘텐츠가 셀 영역 위로 잘려 표시됨. 인덱스 행 (rs=1, h=1980) 도 동일 결함 — 셀 내부 paragraph 누적 높이가 IR `cell.h` 와 정합 안됨.

## 핵심 영역 (archives 이력 분석 결과)

| Task | 영역 | 본 회귀 관련성 |
|------|------|---------------|
| **#214** ★ | 단일 패스 레이아웃 엔진 — TypesetEngine 전환 | 셀 높이 계산 path 가 `height_measurer` → `typeset` 이동 |
| **#185** | HeightMeasurer line_height 보정 (raw_lh < max_fs) | typeset 경로에서 일관 적용 점검 필요 |
| **#102** | 재귀적 높이 측정 (cell_controls_height) | 중첩 표 + multi-paragraph 셀 콘텐츠 누적 |
| **#324** | compute_cell_line_ranges 누적위치 재작성 | 셀 line range 측정 정합 |
| **#325** | cell.h vs 실제 콘텐츠 누적 max() 정책 | 본 회귀와 동일 본질 (close 됨, 자연 해소 추정) |
| **#431** | 분할 표 셀 내 문단 미출력 (단위 mismatch) | HU/px 단위 정합 |

★ = 회귀 origin 가능성 최우선 영역

## 단계 분리 (5 stages)

### Stage 1 — 회귀 본질 진단

- `dump-pages` / `dump` / `export-svg --debug-overlay` 로 mel-001 p2 s0:pi=22 의 IR 정합 확인
- IR `cell.h` (HU/px) ↔ 실제 paragraph 누적 높이 측정 비교
- 회귀 범위 좁히기:
  - 현재 typeset 경로에서 셀 콘텐츠 누적 위치
  - paragraph_layout / typeset_block_table 의 multi-paragraph 셀 처리 path
- bisect 후보 영역 (크롬 v0.2.1 5665d49 ↔ devel HEAD) 좁히기 — 진단 결과로 회귀 origin commit 후보 1~2개 좁힘

**산출물**: `mydocs/working/task_m100_501_stage1.md` (진단 보고서)

### Stage 2 — 회귀 origin 확정 + 정정 방향

- bisect 또는 직접 분석으로 회귀 origin commit 확정
- 정정 영역 결정 — 후보:
  - typeset 경로의 셀 콘텐츠 높이 누적 (cell.h vs 측정 max)
  - HeightMeasurer 보정 (#185) 의 typeset 일관 적용
  - compute_cell_line_ranges 누적 (#324) 정정
  - 1x1 래퍼 / multi-paragraph 측정 (#102 재귀)

**산출물**: `mydocs/working/task_m100_501_stage2.md` + 구현 계획서 `mydocs/plans/task_m100_501_impl.md` 작성 → 승인 게이트

### Stage 3 — 정정 구현 + 단위 테스트

- Stage 2 의 결정에 따라 정정 구현
- 단위 테스트 (mel-001 p2 셀 높이 회귀 방지)
- cargo test --lib 회귀 0
- cargo clippy --lib -- -D warnings 0건

**산출물**: `mydocs/working/task_m100_501_stage3.md`

### Stage 4 — 광범위 회귀 점검

- 다른 샘플 시각 + byte 비교 회귀 0:
  - exam_kor / exam_science / exam_social / exam_eng / 21_언어_기출
  - synam-001 / k-water-rfp / kps-ai / aift / hwpspec
  - mel-001 p2 (정정 대상) + 다른 페이지 회귀 0
- WASM 빌드 + 크기 점검

**산출물**: `mydocs/working/task_m100_501_stage4.md`

### Stage 5 — 작업지시자 시각 검증 + 최종 보고서

- mel-001 p2 디버그 오버레이 시각 비교 (회귀 ↔ 정정 후)
- 작업지시자 시각 판정 게이트
- 최종 보고서 + 오늘할일 갱신
- 이슈 #501 close 승인 요청

**산출물**: `mydocs/report/task_m100_501_report.md` + `mydocs/orders/{날짜}.md` 갱신

## 검증 게이트 (각 Stage 공통)

| 검증 | 기준 |
|------|------|
| cargo test --lib | 회귀 0 (현재 baseline 1086+) |
| cargo test --test svg_snapshot | 6/6 |
| cargo test --test issue_418 | 1/1 |
| cargo clippy --lib -- -D warnings | 0건 |
| WASM 빌드 | 정합 |
| **작업지시자 시각 검증** | mel-001 p2 + 다른 샘플 회귀 0 (Stage 5 게이트) |

## 위험 영역

- **셀 높이 영역 = 다수 정정 사이클 누적 영역** (트러블슈팅 2026-02-17, #102, #185, #198, #214, #215, #324, #325, #347, #431) — 정정이 다른 영역 회귀 유발 가능성
- **TypesetEngine 전환 (#214) 후 layered pipeline 다수 영역 의존** — 본 영역 정정이 다른 샘플 (synam-001 / k-water-rfp / aift 등) 회귀 유발 가능성 높음
- 따라서 Stage 4 (광범위 회귀 점검) 가 핵심 게이트

## 처리 정합 (메인테이너 정책)

- 일반화 알고리즘 흡수 시 회귀 위험 강함 — `feedback_hancom_compat_specific_over_general.md` 메모리 룰 적용 (구조 가드 우선)
- v0.7.6 사이클 회귀 origin 가능성 — `feedback_v076_regression_origin.md` 메모리 룰 적용 (작업지시자 직접 시각 검증 게이트)

## 다음 단계

작업지시자 승인 후 Stage 1 (회귀 본질 진단) 진행.
