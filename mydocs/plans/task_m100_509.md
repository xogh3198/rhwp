# Task #509 수행 계획서 — draw_text 의 PUA 강제 변환 회귀

## 이슈 정합

- **Issue**: [#509](https://github.com/edwardkim/rhwp/issues/509) — bug, assignee = edwardkim
- **회귀 origin**: [PR #251](https://github.com/edwardkim/rhwp/pull/251) (seanshin/feature/pua-symbol-border-margin)
- **본질**: `map_pua_bullet_char` 가 bullet 발행 path 외 `draw_text` (모든 텍스트) 에도 적용 → 일반 텍스트의 PUA 영역 회귀

## 본 사이클 조사 결과

### 회귀 origin 정합 — PR #251

본 PR 의 정정 의도:
- Wingdings 등 심볼 폰트의 PUA 영역 (U+F000~F0FF) 글자 → 유니코드 표준 변환
- bullet (글머리표) 영역 한정 정정 의도

본 PR 의 부작용:
- 함수명 `map_pua_bullet_char` (bullet 의도) 가 **모든 `draw_text` 호출** 에 적용
- 일반 텍스트의 PUA 영역 (작성자 폰트 직접 매핑) 도 강제 변환

### 현재 코드 영역 점검

| 위치 | 호출 컨텍스트 | 정합 |
|------|---------------|------|
| `src/renderer/svg.rs:1851` | `draw_text` (모든 텍스트) | **회귀 영역** ★ |
| `src/renderer/web_canvas.rs:1444` | `draw_text` (모든 텍스트) | **회귀 영역** ★ |
| `src/renderer/html.rs:259` | `draw_text` (모든 텍스트) | **회귀 영역** ★ |
| `src/renderer/layout/paragraph_layout.rs:2849` | bullet 발행 path | 정합 (의도 유지) |
| `src/wasm_api.rs:4019, 4157` | bullet 발행 path | 정합 (의도 유지) |

### `map_pua_bullet_char` 함수 본질

`src/renderer/layout/paragraph_layout.rs:2909` 의 함수 정의:
```rust
/// HWP PUA 문자(0xF000~0xF0FF)를 표준 Unicode로 매핑
/// 기준: Wingdings 폰트 → Unicode 매핑 (alanwood.net/demos/wingdings.html)
/// HWP 글머리표는 Wingdings 폰트 문자를 PUA(0xF000+code)로 저장
pub(crate) fn map_pua_bullet_char(ch: char) -> char {
    let code = ch as u32;
    if !(0xF020..=0xF0FF).contains(&code) {
        return ch;
    }
    let w = (code - 0xF000) as u8;
    match w {
        // 도형/기호 (0x6C~0x7E) — Wingdings 매핑
        0x6C => '\u{25CF}', // ● Black circle
        ...
    }
}
```

**함수 docstring 정합**: "HWP **글머리표** 는 Wingdings 폰트 문자를 PUA(0xF000+code)로 저장" — 즉 함수 자체는 bullet (글머리표) 영역을 위한 것.

### 정정 방향 — 옵션 A (권장)

**`draw_text` 의 PUA 변환 제거 + bullet 발행 path 만 보존**

근거:
1. 함수 정의 자체가 "bullet" 영역 한정 (docstring + 함수명)
2. bullet 발행 path (paragraph_layout:2849, wasm_api:4019/4157) 이미 본 함수 호출 → 정정 효과 보존
3. `draw_text` 일반 적용은 메모리 룰 `feedback_hancom_compat_specific_over_general` 위반 (구조 가드 우선)
4. 작성자가 폰트 직접 PUA 매핑한 케이스 회귀 차단

## 단계 분리 (5 stages)

### Stage 1 — 회귀 본질 진단 + 샘플 수집

- 작업지시자가 본 회귀 시각 증상 + 재현 샘플 제공
- 회귀 영역의 정확한 PUA 코드포인트 + 폰트 매핑 패턴 확인
- 본 task 의 정정 영역이 회귀 차단 정합인지 점검

**산출물**: `mydocs/working/task_m100_509_stage1.md`

### Stage 2 — Red 테스트 추가

- 통합 테스트 (`tests/issue_509.rs`) — 일반 텍스트 PUA 영역의 미변환 보장
- bullet 영역의 PUA 변환 보존 확인 (회귀 0)

**산출물**: `tests/issue_509.rs` 신규

### Stage 3 — 정정 구현

- `src/renderer/svg.rs::draw_text` 의 `map_pua_bullet_char` 호출 제거
- `src/renderer/web_canvas.rs::draw_text` 동일
- `src/renderer/html.rs::draw_text` 동일
- bullet 발행 path 영역 보존 (paragraph_layout:2849, wasm_api:4019/4157)

**산출물**: 3 파일 변경 + Red → Green 검증

### Stage 4 — 광범위 회귀 점검

- cargo test --lib (1102+ baseline 회귀 0)
- svg_snapshot 6/6 — bullet 영역 정합 보존
- issue_418 / issue_501 PASS — 본 사이클 정정 영역 회귀 0
- 광범위 byte 비교: exam_kor / exam_science / exam_eng / 21_언어_기출 / synam-001 / k-water-rfp / aift / mel-001 / hwpspec / hwp3-sample
- bullet 사용 샘플 (Wingdings 폰트 영역) 회귀 0 확인

**산출물**: `mydocs/working/task_m100_509_stage4.md`

### Stage 5 — 작업지시자 시각 검증 + 최종 보고서

- 회귀 영역 정정 후 시각 비교 (작업지시자 제공 샘플)
- bullet 영역 정합 보존 시각 확인
- 트러블슈팅 + (필요 시) 위키 작성
- 최종 보고서 + 오늘할일 갱신
- 이슈 #509 close 승인

**산출물**: `mydocs/report/task_m100_509_report.md`

## 검증 게이트 (각 Stage 공통)

| 검증 | 기준 |
|------|------|
| cargo test --lib | 회귀 0 (baseline 1105+) |
| cargo test --test svg_snapshot | 6/6 |
| cargo test --test issue_418 | 1/1 |
| cargo test --test issue_501 | PASS |
| cargo clippy --lib -- -D warnings | 0건 |
| WASM 빌드 | 정합 |
| **작업지시자 시각 검증** | 회귀 영역 정정 + bullet 영역 보존 (Stage 5 게이트) |

## 위험 영역

- **bullet 영역 회귀 위험** — `map_pua_bullet_char` 호출 path 분리 시 bullet 발행 path 의 호출 누락 가능성
- **Wingdings 폰트 정합 영역** — 본 정정으로 일반 텍스트의 Wingdings 매핑 회피 영역 시각 점검 필수
- 메모리 룰 `feedback_hancom_compat_specific_over_general` — 일반화 회피, 구조 가드 우선

## 처리 정합 (메모리 룰)

- `feedback_hancom_compat_specific_over_general` — bullet 컨텍스트 한정 가드 (일반화 회피)
- `feedback_v076_regression_origin` — 작업지시자 직접 시각 검증 게이트
- `feedback_search_troubleshootings_first` — 본 사이클 시작 전 troubleshootings/ 전수 검색

## 다음 단계

작업지시자 승인 후 Stage 1 (회귀 본질 진단 + 샘플 수집) 진행.
