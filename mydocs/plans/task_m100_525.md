# Task #525 수행 계획서 — exam_science 8번 문제 인라인 수식+한글 중첩

## 1. 배경

이슈 #525 ("exam_science 8번 문제 인라인 화학식 글자 겹침"): pi=37 본문에서 한글-수식 글자 박스 1.3~9px 다중 중첩. 이슈 본문 가설은 "Shape advance 폭 부족" 또는 "ParaShape spacing 보정 누락".

## 2. 사전 진단 결과 — 이슈 가설 정정

본 task 시작 전 빠른 진단 (`/tmp/task525_p2/exam_science_002.svg` SVG 좌표 추출) 으로 **본질이 이슈 가설과 다름** 을 확정:

### 2-1. 본질: 마지막 2 줄 중복 렌더링

pi=37 의 8 줄 중 **마지막 2 줄 (ls[6], ls[7]) 만 모든 글자가 두 위치에 emit**. 다른 줄(ls[0]~ls[5])은 정상 (각 글자 1 위치).

| ls | vpos | sw (HU) | y (px) | 상태 |
|----|------|---------|--------|------|
| 0 | 35652 | 17546 | 639.41 | 정상 (그림 옆 좁은 영역) |
| 1 | 37542 | 17546 | 662.35 | 정상 |
| 2 | 39152 | 17546 | 683.81 | 정상 |
| 3 | 40762 | 17546 | 705.28 | 정상 |
| 4 | 42372 | 17546 | 729.41 | 정상 |
| 5 | 44182 | 17546 | 753.55 | 정상 |
| **6** | **45792** | **31692** | **775.01** | **모든 글자 중복 (sw=전체 column)** |
| **7** | **47402** | **31692** | **796.48** | **모든 글자 중복 (sw=전체 column)** |

### 2-2. 중복 패턴 (y=775.01 예시)

각 글자가 **두 x 위치**에 출력 — 첫 시퀀스 + 둘째 시퀀스가 같은 줄에 동시 emit:

| 글자 | 첫 시퀀스 x | 둘째 시퀀스 x | 차 |
|------|-------------|---------------|-----|
| 옥 | 139.28 (n=2) | 143.74 (n=1) | +4.46 |
| 텟 | 153.08 (n=2) | 157.54 (n=1) | +4.46 |
| 규 | 170.52 (n=2) | 179.45 (n=1) | +8.93 |
| 칙 | 184.32 (n=2) | 193.25 (n=1) | +8.93 |
| 을 | 198.12 (n=2) | 207.05 (n=1) | +8.93 |
| ... | ... | ... | ... (누적) |

(n=2 = SVG outline+fill 정상 패턴, n=1 = 추가 emit). y=796.48 도 동일 패턴.

### 2-3. 핵심 단서

- pi=37 ci=0 = **Picture (그림) wrap=Square**, vert_align=Top, horz=Right, 47×33mm
- ls[5] 까지 sw=17546 (그림 옆 좁은 영역), ls[6]~7 sw=31692 (그림 아래 전체 column)
- `layout.rs:2050, 2183` 의 **`is_wrap_host` 가드는 `Control::Table` 만 검사**, `Control::Picture/Shape` 의 wrap=Square 는 누락
- 결과: Picture wrap=Square host paragraph 도 `wrap_around_paras` 경로에서 텍스트 emit + `PartialParagraph/FullParagraph` 경로에서 동일 줄 또 emit → ls[6]~7 중복

### 2-4. 영향 범위

본 샘플 (exam_science) 내 동일 패턴 (Picture wrap=Square + tac=false + 다행 paragraph) 단락이 더 있을 가능성. Stage 1 진단에서 광범위 조사 필요.

## 3. 가설

A. `is_wrap_host` 검사 누락 (Picture/Shape wrap=Square) → `layout.rs:2050, 2183` 패턴 보강.
B. `wrap_around_paras` 가 pi=37 를 포함하지 않으나, 그림 옆 좁은 영역 처리는 **다른 코드 경로** (예: TypesetEngine 의 Square wrap 그림 host 로직, Task #524 와 연관) 가 담당. 정정 위치가 다를 수 있음.

→ Stage 1 에서 정확한 코드 경로 식별 후 가설 좁히기.

## 4. 단계 (4단계)

### Stage 1 — 본질 코드 경로 확정

(a) `RHWP_LAYOUT_DEBUG` 또는 임시 eprintln 으로 pi=37 가 어떤 layout 함수 호출 받는지 추적:
   - `layout_paragraph` / `layout_partial_paragraph` / `layout_wrap_around_paras` / `layout_inline_table_paragraph` / Square wrap typeset 경로
(b) ls[6]~7 만 중복되는 정확한 분기 식별 (전체 줄 중복 vs 후미 줄 중복)
(c) 영향 범위 광범위 조사 — 7 샘플 SVG 에서 동일 중복 패턴 검색
(d) `mydocs/working/task_m100_525_stage1.md` 작성 + 정정안 후보 (A안/B안)

**완료 기준**: pi=37 ls[6]~7 중복의 정확한 코드 경로 + 영향 범위 식별.

### Stage 2 — 정정

진단 결과에 따라:
- A안: `is_wrap_host` 가드에 Picture/Shape wrap=Square 추가 (layout.rs:2050, 2183, 1639 영역)
- B안: TypesetEngine Square wrap Picture 경로의 ls[6]~7 emit 제거 (Task #524 영역)
- C안: Pagination 영역 — wrap_around_paras 에 Picture host 포함

**완료 기준**: pi=37 ls[6]~7 의 둘째 시퀀스 emit 0 건. 첫 시퀀스(정상) 유지.

### Stage 3 — 회귀 검증

- `cargo test --lib` (1122+ pass)
- `cargo clippy --release --lib -- -D warnings` (warning 0)
- `scripts/svg_regression_diff.sh build HEAD~1 HEAD` (7 샘플 byte-identical 검증)
- exam_science 변경 페이지 = 의도된 정정 (pi=37 + Stage 1 에서 발견된 동일 패턴 단락) 만
- 다른 샘플 회귀 0 건

**완료 기준**: 다른 샘플 byte-identical, exam_science 변경은 의도된 정정만.

### Stage 4 — 최종 보고서 + close

- `mydocs/report/task_m100_525_report.md`
- `mydocs/orders/20260503.md` 갱신 (#525 행 → 완료)
- merge: `local/task525` → `local/devel` → `devel` (push)
- `gh issue close 525`

## 5. 위험·대응

| 위험 | 영향 | 대응 |
|------|------|------|
| Picture wrap=Square 가드 추가 시 다른 정상 paragraph 회귀 | wrap=Square 그림이 있는 모든 paragraph 영향 | svg_regression_diff 7 샘플 검증. wrap=Square Picture 가 거의 없는 본 샘플들에서 회귀 가능성 낮음. |
| ls[6]~7 첫 시퀀스도 함께 누락 | 본문 텍스트 자체 사라짐 (이슈가 더 심각해짐) | Stage 2 후 첫 시퀀스 좌표 (139.28, 121.84 등) 가 보존되는지 명시 검증. |
| TypesetEngine 영역 변경 시 pagination 회귀 | 페이지 수 변경 (Task #524 와 같은 영역) | 페이지 수 무변경 검증. |
| Task #524 의 비-TAC Picture wrap 경로와 충돌 | 회귀 위험 | Stage 1 에서 Task #524 commit (00e152f) 이 어느 경로를 변경했는지 식별 후 정합. |

## 6. 검증 게이트

| 게이트 | 도구 | 기준 |
|--------|------|------|
| 단위 테스트 | `cargo test --lib --release` | 1122+ pass |
| Clippy | `cargo clippy --release --lib -- -D warnings` | warning 0 |
| 회귀 검증 | `scripts/svg_regression_diff.sh` | 7 샘플 byte-identical (의도 정정 OK) |
| 시각 정합 | export-svg + 작업지시자 PDF 비교 | exam_science p2 좌측 단 8번 문제 ls[6]~7 중복 0 건 |

## 7. 참고

- 이슈 #525 본문 가설 ("Shape advance 폭" / "ParaShape spacing") 은 본 진단으로 부정확 확정. 새 본질 (중복 렌더링) 적용.
- 관련 영역: Task #524 (Square wrap Picture anchor 정정 — 비-TAC 분기), `layout.rs:2050, 2183`, `typeset.rs::wrap_around_pic_bottom_px`.
- 관련 commit: 00e152f (Task #524 Stage 2-A — `para_top_y_for_pic_anchor` 보존).

---

승인 요청: 본 진단 결과 + 갱신된 본질 (중복 렌더링) + 4단계 진행 가능 여부.
