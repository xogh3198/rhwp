# Task #534 수행계획서 — exam_kor 18p 우측 단 [A]/[B] + 인라인 그림 위치 결함

**작성일**: 2026-05-02
**이슈**: [#534](https://github.com/edwardkim/rhwp/issues/534)
**브랜치**: `local/task534`
**선행**: 없음 (#533 비관여 확인 완료)

## 1. 본질 (현재 가설)

`samples/exam_kor.hwp` 18페이지 우측 단 pi=50 [A] / pi=56 [B] 표시기 옆의 인라인 그림 (TAC) 의 x 좌표가 우측으로 과다하게 밀려 단 영역을 30-70% 침범. 같은 paragraph 안에 비-TAC Square wrap 인라인 표 + tac=true 그림 이 동시 배치된 케이스에서 그림 x 좌표 계산 결함 추정.

## 2. 측정 데이터

우측 단 col_area: x ≈ 605, width ≈ 437.

| pi | 패턴 | 그림 x 시작 | 그림 x 끝 | 단 영역 침범 |
|----|------|-----------|-----------|-------------|
| pi=46 | 단독 그림 | 850 | 1157 | +115 px |
| pi=50 | 표 + 그림 | **981** | **1349** | **+307 px ★** |
| pi=54 | 단독 그림 | 740 | 1018 | 정상 |
| pi=56 | 표 + 그림 | **945** | **1313** | **+271 px ★** |

## 3. 가설 영역

| 가설 | 위치 | 우선순위 |
|------|------|---------|
| Picture (tac=true) x 가 같은 paragraph 의 Square wrap 인라인 표 폭/위치를 잘못 가산 | `src/renderer/layout.rs::layout_table_item` Square wrap 분기 또는 `paragraph_layout.rs` TAC Picture | 높음 |
| Parser 단의 horz_offset / cur size 음수 (i32) 처리 결함 (u32 wraparound 발견) | `src/parser/hwp5/...` Picture/Shape | 중 |
| TAC 그림 의 정렬 (Left) + 단 폭 가산이 표의 영역만큼 더해짐 | `paragraph_layout.rs` 인라인 폭 계산 | 중-높음 |

## 4. Stage 정의

| Stage | 산출물 |
|-------|-------|
| Stage 1 | Root cause 위치 확정 — 보고서 (코드 변경 없음) |
| Stage 2 | 구현계획서 작성 — 작업지시자 승인 |
| Stage 3 | 코드 적용 + 단위 테스트 |
| Stage 4 | 광범위 회귀 검증 (`scripts/svg_regression_diff.sh`) |
| Stage 5 | 시각 판정 (작업지시자) + 최종 보고서 |

## 5. 회귀 위험

| 영역 | 위험 |
|------|------|
| Picture (tac=true) 단독 케이스 | 같은 paragraph 안에 표가 없는 케이스 (pi=46/54 패턴) — 회귀 차단 필요 |
| Square wrap 표 단독 케이스 | 그림이 없는 케이스 — 회귀 차단 필요 |
| 다른 controls 조합 (Shape + 표 + 그림 등) | 광범위 샘플 회귀 측정 필수 |

## 6. 메모리 정합

- `feedback_essential_fix_regression_risk` — 같은 paragraph 의 controls 상호작용 본질 정정은 다양한 조합 영역 회귀 위험 큼 → 광범위 샘플 회귀 검증 필수
- `feedback_pdf_not_authoritative` — PDF 비교는 보조 reference. 작업지시자 시각 판정 우선
- `feedback_rule_not_heuristic` — 분기/허용오차 도입 전 룰/휴리스틱 자문 필요

## 7. 산출물 계획

| 산출물 | 위치 |
|--------|------|
| 수행계획서 | `mydocs/plans/task_m100_534.md` (본 문서) |
| 구현계획서 | `mydocs/plans/task_m100_534_impl.md` (Stage 1 직후) |
| Stage 보고서 | `mydocs/working/task_m100_534_stage{1..5}.md` |
| 최종 보고서 | `mydocs/report/task_m100_534_report.md` |

## 8. 승인 요청

작업지시자 승인 후 Stage 1 (root cause 위치 확정 — 코드 변경 없음) 진행.
