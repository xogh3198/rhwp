# Task #546 수행 계획서 — exam_science.hwp 2페이지 페이지네이션 회귀

## 이슈 정합

- **이슈**: [#546](https://github.com/edwardkim/rhwp/issues/546) — exam_science.hwp 2페이지 페이지네이션 회귀 — PR #506 머지 후 본문 누락 (4페이지 → 6페이지)
- **마일스톤**: v1.0.0 (M100)
- **assignee**: edwardkim
- **선행 의존성**: PR #506 (이미 머지됨, devel `c7330cf`)

## 본질 (1차 진단)

### bisect 결과 — 회귀 origin 정확히 확정

| 시점 | commit | 총 페이지 | p2 단 0 items / used | 상태 |
|------|--------|----------|---------------------|------|
| v0.7.9 (main) | `0fb3e675` | 4 | 37 / 1133.6 px | ✅ 정상 |
| **PR #506 직전** | `65e275d` | **4** | 37 / 1133.6 px | ✅ 정상 |
| **PR #506 머지 후** | `c7330cf` | **6** | 2 / 132.7 px | ❌ **회귀 origin** |

본 환경 PR #538 cherry-pick 후 (현재 devel `e585b58`) 시점에서도 동일한 회귀 잔존 (PR #538 와 무관 확인됨).

### 결함 본질

`samples/exam_science.hwp` 페이지 2 의 본문 (37 items: paragraph + 표 + 수식 등) 이 표 2개 (`pi=30 ci=0` 3x3 + `pi=31 ci=0` 1x10) 만 남고 나머지가 다른 페이지로 분리됨. 총 페이지 4 → 6 으로 증가.

```
=== PR #506 직전 (정상) ===
페이지 2 — 단 0 (items=37, used=1133.6px)
  FullParagraph  pi=32  vpos=0  "7.-다음은 학생 가 수행한 탐구 활동이다."
  Shape          pi=32 ci=0  수식  vpos=0
  Table          pi=33 ci=0  1x1  407.4x269.4px  wrap=TopAndBottom tac=true  vpos=2390
  ... (37 items 정상 배치)

=== PR #506 머지 후 (회귀) ===
페이지 2 — 단 0 (items=2, used=132.7px)
  Table          pi=30 ci=0  3x3  407.4x107.1px  wrap=TopAndBottom tac=true  vpos=78952
  Table          pi=31 ci=0  1x10  411.9x19.1px  wrap=TopAndBottom tac=true  vpos=87944
  (본문 35 items 모두 다른 페이지로 분리됨)
```

## 작업 목표

### 본질

**PR #506 의 51 commits 중 회귀 origin 정확히 식별 + 정정**.

### 정답지

- **v0.7.9 (`0fb3e675`)** 의 4 페이지 + p2 본문 37 items 가 정합 정답지
- 한컴 2010 + 한컴 2022 (작업지시자 직접 시각 판정으로 정합 재확인)

## 범위

### 포함

| 영역 | 내용 |
|------|------|
| 1. PR #506 51 commits 정밀 bisect | 회귀 origin commit 정확히 식별 |
| 2. 회귀 영역 정밀 분석 | 식별된 commit 의 변경 영역 + 회귀 본질 식별 |
| 3. 정정 (옵션 결정) | 회귀 commit revert / 부분 revert / 별도 정합 정정 |
| 4. 회귀 검증 | exam_science 4 페이지 복원 + svg_snapshot + 광범위 fixture 회귀 0 |
| 5. 시각 판정 | 작업지시자 한컴 2010/2020 직접 판정 |

### 제외

- PR #506 머지 자체 revert (광범위 영향, 다른 task #417/#460 의 본질 정정 보존 필요)
- HWP 3.0 파서 영역 (Task #417) — exam_science 는 HWP5/HWPX 추정 (별도 본질)
- 다른 fixture 의 다른 회귀 (별도 task)

## 단계 분리 (5 stages)

### Stage 1 — 회귀 origin commit 정확히 식별 (binary bisect)

**목표**: PR #506 의 51 commits 중 회귀 발생 commit 정확히 식별.

**방법**: `git bisect` 자동화

```bash
git bisect start
git bisect bad c7330cf       # PR #506 머지 후 (회귀)
git bisect good 65e275d      # PR #506 직전 (정상)
# 각 단계마다:
# cargo build --release --bin rhwp
# rhwp dump-pages samples/exam_science.hwp -p 1 | head -3
# "6페이지" 면 git bisect bad, "4페이지" 면 git bisect good
# log2(51) ≈ 6 단계로 정확히 좁혀짐
```

**우선순위 후보** (bisect 시 우선 점검):

| commit | 영역 | 가능성 |
|--------|------|--------|
| `82e41ba` | Task #460 보완5: Square wrap 그림 아래 텍스트 y위치 보정 (layout + typeset) | 🟥 매우 높음 |
| `9d9b4ed` | Task #460 보완4: Square wrap 그림 어울림 텍스트 렌더링 수정 | 🟥 매우 높음 |
| `0aa7a5e` | Task #460: Square wrap 레이아웃 수정 + HWPX TextRunReflow 자동 보정 | 🟧 중간 |
| `5fcea4f` | Task #477: 표 셀 안 그림 클램프 + 이미지 Crop Scale 룰 정합화 | 🟧 중간 |
| `d97cdf9` | Task #474: RowBreak 표 보호 블록 정책 비적용 | 🟧 중간 |
| `febfc72` | Task #460 Stage 5: HWP3 LINE_SEG line_spacing=0 수정 (표 텍스트 겹침 해소) | 🟢 작음 (HWP3 만) |

**산출물**: `mydocs/working/task_m100_546_stage1.md`

**완료 기준**: 회귀 origin commit 단일 식별. 작업지시자 승인.

### Stage 2 — 구현 계획서 작성

**목표**: 식별된 commit 의 회귀 본질 정밀 분석 + 정정 정책 확정.

**가능한 정정 옵션**:

| 옵션 | 내용 | 영향 |
|------|------|------|
| **A. revert (단일 commit)** | 회귀 commit 만 revert | 본 task 의 결함 해소 + 그 commit 의 의도된 정정 손실 가능 |
| **B. 부분 revert** | 회귀 본질 영역만 부분 revert (혼합 commit 인 경우) | 정밀 정정, 다만 의존성 상호작용 점검 필요 |
| **C. 별도 정합 정정** | 회귀를 보존하면서 exam_science 의 페이지네이션 본질을 별도 분기로 정정 | 가장 안전, 다만 코드 영역 확장 |

**산출물**: `mydocs/plans/task_m100_546_impl.md`

**완료 기준**: 정정 정책 확정 (옵션 A/B/C). 작업지시자 승인.

### Stage 3 — 정정 적용

**목표**: 정정 정책 적용 + 단위 검증.

**산출물**: `mydocs/working/task_m100_546_stage3.md`

### Stage 4 — 회귀 검증

**목표**:
1. exam_science.hwp 4 페이지 + p2 단 0 items=37 / used=1133.6 px 복원
2. cargo test --lib (1113+ 통과)
3. cargo test --test svg_snapshot (6/6 통과)
4. cargo test --test issue_530/505/418/501 (회귀 0)
5. cargo clippy --lib -- -D warnings (0 건)
6. 광범위 fixture 회귀 0 (다른 fixture 의 페이지 수 / svg_snapshot 영향 점검)

**산출물**: `mydocs/working/task_m100_546_stage4.md`

### Stage 5 — 시각 판정 + 최종 보고

**목표**:
1. 작업지시자 시각 판정 (rhwp 자체 해석 권위):
   - exam_science.hwp p2 정상 출력 (4 페이지 / 본문 37 items 정합)
   - 한컴 2010/2020 의 동일 fixture 와 비교
2. WASM 빌드 + studio 동기화
3. 시각 판정 통과 후:
   - 최종 보고서 (`mydocs/report/task_m100_546_report.md`)
   - orders 갱신 (20260503.md 또는 다음 날짜)
   - local/task546 → local/devel merge → devel push → 이슈 close

**산출물**: `mydocs/report/task_m100_546_report.md`

## 위험 영역

| 위험 | 가능성 | 회피책 |
|------|--------|--------|
| 회귀 origin commit 의 의도된 정정 손실 | 🟧 중간 | Stage 2 정정 정책 결정 시 옵션 B (부분 revert) 또는 C (별도 정합) 우선 검토 |
| 광범위 fixture 회귀 (PR #506 의 다른 영역 정정 영향) | 🟧 중간 | Stage 4 의 광범위 fixture sweep + svg_snapshot 6/6 |
| HWP3 vs HWP5/HWPX 분기 결함 (exam_science 가 어느 포맷인지) | 🟢 작음 | Stage 1 bisect 결과로 식별 |
| 시각 판정 미통과 (h.exam_science 의 정합 시각 한컴 출력과 다름) | 🟢 작음 | rhwp 자체 시각 해석 권위 메모리 적용 |

## 메모리 정합

- `feedback_process_must_follow` — 절차 준수
- `feedback_assign_issue_before_work` — 이슈 #546 메인테이너 assign 완료 ✅
- `feedback_check_open_prs_first` — 본 task 와 충돌 가능 PR 사전 점검 (현재 open 외부 PR 없음)
- `feedback_search_troubleshootings_first` — Stage 1 직전 troubleshootings 폴더 사전 검색 (Square wrap / pagination / exam_science 영역) ✅
- `feedback_v076_regression_origin` — bisect 로 회귀 origin 정확히 좁힘 (작업지시자 환경 직접 검증)
- `feedback_visual_regression_grows` — Stage 5 시각 판정 게이트 필수
- `feedback_pdf_not_authoritative` / `reference_authoritative_hancom` — 한컴 2010/2020 직접 판정으로 정답지 확정
- `feedback_hancom_compat_specific_over_general` — 정정은 case-specific (exam_science 의 본질 한정) 우선
- `feedback_commit_reports_in_branch` — task 브랜치에서 commit
- `feedback_close_issue_verify_merged` — close 전 devel 머지 검증

## 승인 게이트

1. 본 수행 계획서 승인 → Stage 1 bisect
2. Stage 1 보고서 승인 → 구현 계획서
3. 구현 계획서 승인 → Stage 3 정정
4. Stage 3/4 보고서 승인 → Stage 5 시각 판정
5. Stage 5 시각 판정 승인 → 최종 보고 + merge + close

## 다음 단계

작업지시자 본 수행 계획서 승인 후 Stage 1 (binary bisect) 진행.
