# Task #477 Stage 4 — 작업지시자 시각 검증

## WASM 빌드

```
docker compose --env-file .env.docker run --rm wasm
```

빌드 결과:
- `pkg/rhwp_bg.wasm`: 4,203,122 bytes (이전 빌드 대비 +3,902 bytes)
- `pkg/rhwp.js`: 230,417 bytes
- 빌드 시간: 1m 20s

## 시각 판정 결과

| 영역 | 결과 |
|------|------|
| **k-water-rfp 1쪽 pi=31** (K-water 로고) | 정상 ✅ — Task #430 회귀 정정 확인 |
| **k-water-rfp 16쪽 pi=186** (두 번째 표 셀 안 그림) | 정상 ✅ — 본 #477 본질 결함 정정 확인 |

작업지시자 응답: *"이번 타스크에서 해결해야 될 표안의 이미지 처리까지 해결되었습니다."*

## 작업지시자 통찰 보존

본 작업 진행 중 작업지시자의 핵심 통찰들:

1. *"컨트리뷰터들 중 이미지 처리쪽 PR 처리 후 발생되는 문제입니다"* — 회귀 origin 가설 (확정)
2. *"이미지 컨트롤의 크기는 같지만, 한컴은 전체 이미지가 커진 것이고, rhwp 는 어떻게 처리하는지 확인합니다"* — 본질 진단
3. *"이미지를 crop 시킨 후 확대하는 잘못된 해석"* — 결함 본질 명확화
4. *"이건 휴리스틱이 아닙니다. 룰입니다."* — 정정 방향 (75 HU/px 표준 룰)
5. *"산수 라는 단어는 너무 원초적입니다. 계산 이라는 표현이 어울립니다."* — 단어 정합 (메모리 보존)

## 다음 단계

- **Stage 5**: 최종 결과보고서 + 오늘할일 갱신 + 머지
